<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>schecterdamien</title>
  
  <subtitle>你从远方来，我到远方去</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://schecterdamien.github.io/"/>
  <updated>2021-03-08T07:43:35.195Z</updated>
  <id>https://schecterdamien.github.io/</id>
  
  <author>
    <name>schecterdamien</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>mysql锁总结</title>
    <link href="https://schecterdamien.github.io/2021/03/04/mysql-lock/"/>
    <id>https://schecterdamien.github.io/2021/03/04/mysql-lock/</id>
    <published>2021-03-04T15:35:01.000Z</published>
    <updated>2021-03-08T07:43:35.195Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp; 最近做了一次和<code>MySQL（innoDB）</code>锁有关的技术分享，记录一下<br>&nbsp;&nbsp;&nbsp;&nbsp; 为了能尽量准确简洁的描述innoDB的锁机制，看了好多的文章，由于innoDB中的锁系统确实非常复杂，细节特别多，如有纰漏和谬误，还请联系改正</p><h3 id="innoDB锁简介"><a href="#innoDB锁简介" class="headerlink" title="innoDB锁简介"></a>innoDB锁简介</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; innoDb支持多种粒度的锁，按照粒度来分，可分为<code>表锁（LOCK_TABLE）</code>和<code>行锁（LOCK_REC）</code><br>&nbsp;&nbsp;&nbsp;&nbsp; 一般的锁系统都会有<code>共享锁</code>和<code>排他锁</code>的分类，<code>共享锁</code>也叫<code>读锁</code>，<code>排他锁</code>也叫<code>写锁</code>。加在同一个资源上，写锁会阻塞另外一把写锁或读锁的获取，读锁则允许另外一把读锁的获取，也就是读读之间允许并发，读写或者写写会阻塞，innodb中表锁和行锁都支持<code>共享锁（简写S）</code>和<code>排他锁（简写X）</code>。<br>&nbsp;&nbsp;&nbsp;&nbsp; 因为innoDB支持多粒度的锁，允许表锁和行锁的并存，为了方便多粒度锁冲突的判断，innoDB中还存在一种名叫<code>意向锁（Intention Locks）</code>的锁。<br>&nbsp;&nbsp;&nbsp;&nbsp; 除此之外，还有一种特殊的表锁，自增锁，主要用来并发安全的生成自增id，一种特殊的意向锁，插入意向锁，用来防止幻读问题</p><h3 id="表锁"><a href="#表锁" class="headerlink" title="表锁"></a>表锁</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 表锁，锁定的粒度是整个表，也分共享锁和排他锁。不同于行锁，表锁MySQL Server层就有实现（所以MyISAM支持表锁，也只支持表锁），innoDb则在存储引擎层面也实现了一遍表锁（后面会介绍具体结构）。<br>&nbsp;&nbsp;&nbsp;&nbsp; 哪些时候会触发表锁呢？在执行某些ddl时，比如<code>alter table</code>等操作，会对整个表加锁，也可以手动执行锁表语句：<code>LOCK TALBES table_name [READ | WRITE]</code>，READ为共享锁，WRITE为排他锁，手动解锁的语句为：<code>UNLOCK TABLES</code>，会直接释放当前会话持有的所有表锁 </p><p>有一些需要注意的地方：</p><ul><li>因为MySQL Server层和InnoDB都实现了表锁，仅当<code>autocommit=0</code>、<code>innodb_table_lock=1</code>（默认设置）时，InnoDB层才能知道MySQL加的表锁，ＭySQL Server才能感知InnoDB加的行锁，这种情况下，InnoDB才能自动识别涉及表级锁的死锁，否则，InnoDB将无法自动检测并处理这种死锁</li><li>在用<code>LOCK TALBES</code>显式获取锁后，只能访问加锁的这些表，并且如果加的是共享锁，那么只能执行查询操作，而不能执行更新操作，如果获得的是排他锁，则可以进行更新操作。</li><li>开始事务时会自动UNLOCK之前的表锁，COMMIT或ROLLBACK都不能释放用<code>LOCK TABLES</code>加的表级锁。<code>LOCK TALBES</code>时会先隐式提交事务，再锁表，<code>UNLOCK TALBES</code>也会隐式提交事务。所以，事务中需要的表锁必须在事务开头一次性获取，无法再事务中间获取，因为不管是<code>LOCK TALBES</code>还是<code>UNLOCK TALBES</code>都会提交事务</li></ul><p>官网上建议的表锁的使用方法：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SET</span> autocommit=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">LOCK</span> <span class="keyword">TABLES</span> t1 WRITE, t2 <span class="keyword">READ</span>, ...;</span><br><span class="line">... <span class="keyword">do</span> something <span class="keyword">with</span> <span class="keyword">tables</span> t1 <span class="keyword">and</span> t2 here ...</span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"><span class="keyword">UNLOCK</span> <span class="keyword">TABLES</span>;</span><br></pre></td></tr></table></figure><p>实际业务中，没有特殊理由，不建议使用表锁，因为锁的粒度太大了，极大的影响并发</p><h3 id="意向锁"><a href="#意向锁" class="headerlink" title="意向锁"></a>意向锁</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;意向锁是一种特殊的表级锁，意向锁是为了让InnoDB多粒度的锁能共存而设计的。取得行的共享锁和排他锁之前需要先取得表的意向共享锁（IS）和意向排他锁（IX），意向共享锁和意向排他锁都是系统自动添加和自动释放的，整个过程无需人工干预。<br>&nbsp;&nbsp;&nbsp;&nbsp;主要是用来辅助表级和行级锁的冲突判断，因为Innodb支持行级锁，如果没有意向锁，则判断表锁和行锁冲突的时候需要遍历表上所有行锁，有了意向锁，则只要判断表是否存在意向锁就可以知道是否有行锁了。表级别锁的兼容性如下表：</p><table><thead><tr><th></th><th></th><th>X</th><th>IX</th><th>S</th><th>IS</th></tr></thead><tbody><tr><td></td><td>X</td><td>Conflict</td><td>Conflict</td><td>Conflict</td><td>Conflict</td><td></td></tr><tr><td></td><td>IX</td><td>Conflict</td><td>Compatible</td><td>Conflict</td><td>Compatible</td><td></td></tr><tr><td></td><td>S</td><td>Conflict</td><td>Conflict</td><td>Compatible</td><td>Compatible</td><td></td></tr><tr><td></td><td>IS</td><td>Conflict</td><td>Compatible</td><td>Compatible</td><td>Compatible</td><td></td></tr></tbody></table><p>可以看到，意向锁与意向锁兼容，IX、IS自身以及相互都兼容，不互斥，因为意向锁仅表示下一层级加什么类型的锁，不代表当前层加什么类型的锁；IX和表级X、S互斥；IS和表级X锁互斥。其兼容性正好体现了其作用</p><h3 id="自增锁"><a href="#自增锁" class="headerlink" title="自增锁"></a>自增锁</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;自增锁是一种特殊的表级别锁，如果一个表的某个行具有<code>AUTO_INCREMENT</code>的列，则一个事务在插入记录到这个表的时候，会先获取自增锁。如果一个事务持有自增锁，会阻塞其他事物对该表的插入操作，保证自增连续。<code>innodb_autoinc_lock_mode</code>变量定义了不同的自增算法，在MySql8.0之前默认值是1，MySql8.0之后默认值是2，具体区别参考<a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-auto-increment-handling.html" target="_blank" rel="noopener">官方文档</a></p><h3 id="行锁"><a href="#行锁" class="headerlink" title="行锁"></a>行锁</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;Innodb中的行锁种类繁多，可以分为：记录锁（record locks）、间隙锁（gap locks）、临键锁（next-key locks），插入意向锁（insert intention locks）。行锁在逻辑上都可以看作作用于索引或者索引间隙之上，索引分为主键索引和非主键索引两种，如果一条sql语句操作了主键索引，MySQL就会锁定这条主键索引；如果一条语句操作了非主键索引，MySQL会先锁定该非主键索引，再锁定相关的主键索引。<br>&nbsp;&nbsp;&nbsp;&nbsp; 很多语句都会加行锁，比如Update、Delete、Insert等操作，或者使用SELECT … FOR SHARE | UPDATE [NOWAIT |SKIP LOCKED]来进行当前读（Locking Reads），其中<code>SHARE</code>表示加共享锁，<code>UPDATE</code>表示加排他锁。当要加的锁与当前行已有锁互斥时，会一直阻塞等待一段时间（<code>innodb_lock_wait_timeout</code>定义了等待时间）。加上<code>NOWAIT</code>参数则不会阻塞，会立即返回，并显示一个错误，加上<code>SKIP LOCKED</code>则会在结果集中跳过这些冲突的记录（慎用）。<br>&nbsp;&nbsp;&nbsp;&nbsp; 在不同的语句，不同的事务隔离级别下，甚至不同的索引类型下，行锁会表现成不同的形式，下面介绍这些形式</p><h5 id="记录锁（record-locks）"><a href="#记录锁（record-locks）" class="headerlink" title="记录锁（record locks）"></a>记录锁（record locks）</h5><p>&nbsp;&nbsp;&nbsp;&nbsp; 在逻辑上，记录锁可以理解为锁定的是某个具体的索引，当SQL执行按照唯一性（Primary key、Unique key）索引进行数据的检索时，查询条件等值匹配且查询的数据是存在，这时 SQL 语句加上的锁即为记录锁</p><img src="/2021/03/04/mysql-lock/record_lock.png"> <h5 id="间隙锁（gap-locks）"><a href="#间隙锁（gap-locks）" class="headerlink" title="间隙锁（gap locks）"></a>间隙锁（gap locks）</h5><p>&nbsp;&nbsp;&nbsp;&nbsp; 在逻辑上，间隙锁可以理解为锁住的是索引之间的间隙，是一个左开右开的区间。当SQL执行按照索引进行数据的检索时，查询条件的数据不存在，这时SQL语句加上的锁即为间隙锁。</p><img src="/2021/03/04/mysql-lock/gap_lock.png"> <p>&nbsp;&nbsp;&nbsp;&nbsp; 如上图，因为这些语句查询的值都不存在，所以锁住的都是间隙。并且在 InnoDb 存储引擎里，每个数据页中都会有两个虚拟的行记录，用来限定记录的边界，分别是：<code>Infimum Record</code> 和 <code>Supremum Record</code>，<code>Infimum</code> 是比该页中任何记录都要小的值，而 <code>Supremum</code> 比该页中最大的记录值还要大，这两条记录在创建页的时候就有了，并且不会删除。所以当查询的值比当前已有记录最大值还大时候，锁住的会是最大值到<code>Supremum</code>之间的间隙。比如第一条语句，查询的时候就算是等值匹配，只要这个不存在的数据落在两个索引节点之间，就算不是一个范围，也会锁住索引节点间的所有数据即gap3，范围（7，11）。</p><p>&nbsp;&nbsp;&nbsp;&nbsp; 间隙锁是可以共存的，共享间隙锁与独占间隙锁之间是没有区别的，两者之间并不冲突。其存在的目的都是防止其他事务往间隙中插入新的纪录，故而一个事务所采取的间隙锁是不会去阻止另外一个事务在同一个间隙中加锁的</p><p>&nbsp;&nbsp;&nbsp;&nbsp; 间隙锁是设计用来防止幻读的，当锁定一个gap时，其他事务没有办法再往这个gap中插入数据，PostgreSQL没有这种机制，所以PostgreSQl没有办法锁住不存在的行，无法防止幻读（见之前的文章：<a href="https://schecterdamien.github.io/2019/04/24/concurrency-problem/">记一次并发问题的排查</a>与<a href="https://schecterdamien.github.io/2019/04/24/pg-mvcc/">PostgreSQL的事务隔离和MVCC</a>）</p><h5 id="临键锁（next-key-locks）"><a href="#临键锁（next-key-locks）" class="headerlink" title="临键锁（next-key locks）"></a>临键锁（next-key locks）</h5><p>&nbsp;&nbsp;&nbsp;&nbsp; 在逻辑上，临键锁可以理解为锁住的是索引本身以及索引之前的间隙，是一个左开右闭的区间。当SQL执行按照非唯一索引进行数据的检索时，会给匹配到行上加上临键锁。</p><img src="/2021/03/04/mysql-lock/next_key_lock.png"> <p>&nbsp;&nbsp;&nbsp;&nbsp; 如上图，当执行<code>select * from table_name where id = 3 for update</code>时会锁定（-∞，3）区间，因为按照这个SQL的语义，即是为了锁住<code>id=3</code>的数据，不允许其他操作，如果只是锁住记录本身，肯定是没有办法保证的，因为这是非唯一索引，还有可能插入其他<code>id=3</code>的数据，如果把间隙都给锁住，则其他对这个间隙的插入操作都会被阻塞，从而保证了一致性，这也是临键锁的用意。</p><p>&nbsp;&nbsp;&nbsp;&nbsp; 如果加锁时，查询条件没有命中索引（非ICP的查询），则InnoDB会尝试给全表每一条记录都加上临键锁，效果相当于锁表了</p><h5 id="插入意向锁（insert-intention-locks）"><a href="#插入意向锁（insert-intention-locks）" class="headerlink" title="插入意向锁（insert intention locks）"></a>插入意向锁（insert intention locks）</h5><p>&nbsp;&nbsp;&nbsp;&nbsp; 插入意向锁是一种间隙锁形式的意向锁，在真正执行INSERT操作之前设置。当执行插入操作时，总会检查当前插入操作的下一条记录（已存在的主索引节点）上是否存在锁对象，判断是否锁住了gap，如果锁住了，则判定和插入意向锁冲突，当前插入操作就需要等待，也就是配合上面的间隙锁或者临键锁一起防止了幻读操作。<br>&nbsp;&nbsp;&nbsp;&nbsp; 因为插入意向锁是一种意向锁，意向锁只是表示一种意象，所以插入意向锁之间不会互相冲突，多个插入操作同时插入同一个gap时，无需互相等待，比如当前索引上有记录4和8，两个并发session同时插入记录6，7。他们会分别为(4,8)加上GAP锁，但相互之间并不冲突。<br>&nbsp;&nbsp;&nbsp;&nbsp; INSERT语句在执行插入之前，会先在gap中加入插入意向锁，如果是唯一索引，还会进行Duplicate Key判断，如果存在相同Key且该Key被加了互持锁，则还会加共享锁，然后等待（因为这个相同的Key之后有可能会回滚删除，这里非常容易死锁）。等到成功插入后，会在这条记录上加排他记录锁。</p><h5 id="行锁小结"><a href="#行锁小结" class="headerlink" title="行锁小结"></a>行锁小结</h5><img src="/2021/03/04/mysql-lock/row_locks.png"> <p>&nbsp;&nbsp;&nbsp;&nbsp; 行锁在不同的语句中和环境条件下可以表现成：记录锁（record locks）、 间隙锁（gap locks）、临键锁（next-key locks）和插入意向锁（insert intention locks）。记录锁锁住具体的记录，间隙锁锁住记录之间的间隙，临键锁锁住记录和记录前面的间隙，插入意向锁则是特殊的间隙锁，在插入前判断行将要插入的间隙是否会有冲突。</p><p>&nbsp;&nbsp;&nbsp;&nbsp; 以上说的各种行锁的加锁情况都是在<code>可重复读（REPEATABLE READ）</code>隔离级别下，这个级别也是innoDB默认的事务隔离级别，是最常用的隔离级别，但是其实不同语句在不同隔离级别下加锁的情况会有非常大的区别，以下会简单说明</p><h3 id="不同语句和隔离级别对加锁的影响"><a href="#不同语句和隔离级别对加锁的影响" class="headerlink" title="不同语句和隔离级别对加锁的影响"></a>不同语句和隔离级别对加锁的影响</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 这里先排除<code>读未提交（READ UNCOMMITTED）</code>这种隔离级别的情况，这种级别在生产上几乎无法使用，会出现脏读的情况，不一致读，无法保证事务的ACID。然后先看下<code>串行化（SERIALIZABLE）</code>隔离级别</p><p>&nbsp;&nbsp;&nbsp;&nbsp; <code>串行化</code>隔离级别和<code>可重复读</code>隔离级别最大的区别应该是，innoDB会隐式的转换所有的SELECT语句，给其加共享锁，变成<code>SELECT ... FOR SHARE</code>，这样读操作会阻塞其他写操作，使得读写无法并发，只能串行，从而保证严格的一致性。不过这种行为也受到<code>autocommit</code>变量的影响：</p><ul><li>如果禁用了<code>autocommit</code>，如上所述，则innoDB会隐式的转换所有的SELECT语句，给其加共享锁</li><li>如果开启了<code>autocommit</code>，不会进行隐式转换，因为每条语句构成一个事务，所有快照读语句（也就是没有FOR UPDATE|SHARE的SELECT语句）可以被认为是只读的事务，是可以安全并发，不需要阻塞其他事务</li></ul><p>&nbsp;&nbsp;&nbsp;&nbsp; <code>不可重复读（READ COMMITTED）</code>隔离级别下，和<code>可重复读</code>隔离级别在行锁方面主要的区别是</p><ul><li><code>不可重复读</code>隔离级别下取消了间隙锁，临键锁也退化成了记录锁。间隙锁定和临键锁仅用于外键约束检查和重复键检查，也就是说加锁时，如果没有符号条件的查询并不加锁，有符合条件的查询也只会给记录加上记录锁。因为没有了间隙锁，所以会出现幻读问题。</li><li>在<code>可重复读</code>隔离级别下，加锁时如果查询条件没有命中索引（非ICP的查询），则会给表中每条记录都加上临键锁。而<code>不可重复读</code>隔离级别下因为没有间隙锁，则会退化成给表中每条数据加上记录锁，并且还会把没有匹配的行上的锁给释放掉，而不是把全表所有记录不管有没有匹配都给锁上</li></ul><h3 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;因为使用表锁时，需要一次性申请所有所需表的锁，所以在只使用表锁的情况下不会出现死锁，一般出现死锁的情况都是行锁。innoDB有死锁探测机制，在申请锁的时候，都会先进行死锁判断，采用的算法深度优先搜索，并且如果在搜索过程中发现有环，就说明发生了死锁，为了避免死锁检测开销过大，如果搜索深度超过了 200（<code>LOCK_MAX_DEPTH_IN_DEADLOCK_CHECK</code>)也同样认为发生了死锁。出现死锁时，innoDB会选择一个回滚代价比较小的事务进行回滚。以下会举几个比较典型的死锁例子(均在可重复度隔离级别下)，首先会先建一张测试的表：</p><figure class="highlight n1ql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> TABLE <span class="symbol">`student`</span> (</span><br><span class="line">  <span class="symbol">`id`</span> int <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line">  <span class="symbol">`uuid`</span> varchar(<span class="number">64</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line">  <span class="symbol">`name`</span> varchar(<span class="number">64</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line">  <span class="symbol">`age`</span> int <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line">  <span class="keyword">PRIMARY</span> <span class="keyword">KEY</span> (<span class="symbol">`id`</span>),</span><br><span class="line">  <span class="keyword">UNIQUE</span> <span class="keyword">KEY</span> <span class="symbol">`uuid_index`</span> (<span class="symbol">`uuid`</span>),</span><br><span class="line">  <span class="keyword">KEY</span> <span class="symbol">`name_index`</span> (<span class="symbol">`name`</span>)</span><br><span class="line">) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 <span class="keyword">COLLATE</span>=utf8mb4_general_ci</span><br></pre></td></tr></table></figure><h5 id="死锁例一"><a href="#死锁例一" class="headerlink" title="死锁例一"></a>死锁例一</h5><table><thead><tr><th>语句顺序\事务</th><th>事务一</th><th>事务二 </th></tr></thead><tbody><tr><td>T1</td><td>begin;</td><td>begin;</td></tr><tr><td>T2</td><td>select * from student where id = 1  for update;</td><td>select * from student where id = 2 for update; </td></tr><tr><td>T3</td><td>select * from student where id = 2  for update;</td><td></td></tr><tr><td>T4（死锁发生）</td><td></td><td>select * from student where id = 1 for update;</td></tr></tbody></table><p>&nbsp;&nbsp;&nbsp;&nbsp;这是最简单最典型的死锁的情况了，两个事务互相锁定持有资源，并且等待对方的资源，最后形成一个环，死锁出现。最后某个事务回滚，写业务代码的时候，应该对并发条件可能出现这种情况的语句有所警觉。</p><h5 id="死锁例二"><a href="#死锁例二" class="headerlink" title="死锁例二"></a>死锁例二</h5><p>前提：事务开始时，<code>student</code>表里有<code>id=1</code>的记录</p><table><thead><tr><th>语句顺序\事务</th><th>事务一</th><th>事务二 </th></tr></thead><tbody><tr><td>T1</td><td>begin;</td><td>begin;</td></tr><tr><td>T2</td><td>select * from student where id = 1  for share;</td><td>select * from student where id = 1 for share; </td></tr><tr><td>T3</td><td>update student set name = ‘Tom’ where id = 1;</td><td></td></tr><tr><td>T4（死锁发生）</td><td></td><td>update student set name = ‘Jack’ where id = 1;</td></tr></tbody></table><p>&nbsp;&nbsp;&nbsp;&nbsp; 两个事务分别对某个记录申请共享锁，因为共享锁性质，两个事务都能获取到。然后又都对这条记录申请排他锁，T3中事务一申请排他锁，等待事务二的共享锁释放，加入锁等待队列，T4中事务二又申请排他锁，于是形成环，死锁条件达成。所以在事务开始时就要想到后面可能会做的操作，提前获取足够强度的锁，而不是中途升级。</p><h5 id="死锁例三"><a href="#死锁例三" class="headerlink" title="死锁例三"></a>死锁例三</h5><p>前提：事务开始时，<code>student</code>表里没有<code>id=100</code>的记录</p><table><thead><tr><th>语句顺序\事务</th><th>事务一</th><th>事务二 </th></tr></thead><tbody><tr><td>T1</td><td>begin;</td><td>begin;</td></tr><tr><td>T2</td><td>select * from student where id = 100 for update;</td><td>select * from student where id = 100 for update; </td></tr><tr><td>T3</td><td>insert into student values (100, ‘uuid100’, ‘jack’, 18);</td><td></td></tr><tr><td>T4（死锁发生）</td><td></td><td>insert into student values (100, ‘uuid100’, ‘jack’, 18);</td></tr></tbody></table><p>&nbsp;&nbsp;&nbsp;&nbsp;如上，在可重复读隔离级别下，如果两个事务同时对某个间隙用<code>SELECT...FOR UPDATE</code>加排他锁，在没有符合该条件记录情况下，两个线程都会加锁成功。程序发现记录尚不存在，就试图插入一条新记录，如果两个线程都这么做，就会出现死锁。因为在记录真正插入之前会加插入意向锁，插入意向锁和间隙锁互斥，所以在T3时，事务一阻塞了，申请插入意向锁排队等待事务二的间隙锁释放，T4时，事务二又申请插入意向锁，需要等待事务一的间隙锁释放，形成环，死锁条件达成。<br>&nbsp;&nbsp;&nbsp;&nbsp;这种情况一般发生在，某些业务需要提前锁住间隙，防止并发插入同一数据（关键属性相同的数据），也就会先<code>SELECT...FOR UPDATE</code>再<code>INSERT</code>，但是这样很容易死锁，可以直接对关键属性建立唯一索引，防止并发插入，也存在无法建立索引的情况<a href="https://schecterdamien.github.io/2019/04/24/concurrency-problem/">记一次并发问题的排查</a>，这时候可以考虑其他办法</p><h5 id="死锁例四"><a href="#死锁例四" class="headerlink" title="死锁例四"></a>死锁例四</h5><p>前提：事务开始时，<code>student</code>表里没有<code>uuid=uuid100</code>的记录</p><table><thead><tr><th>语句顺序\事务</th><th>事务一</th><th>事务二</th><th>事务三</th></tr></thead><tbody><tr><td>T1</td><td>begin;</td><td>begin;</td><td>begin; </td></tr><tr><td>T2</td><td>insert into student values (100, ‘uuid100’, ‘jack’, 18);</td><td></td><td></td></tr><tr><td>T3</td><td></td><td>insert into student values (101, ‘uuid100’, ‘jack’, 18);</td><td>insert into student values (102, ‘uuid100’, ‘jack’, 18);  </td></tr><tr><td>T4（死锁发生）</td><td>rollback;</td><td></td><td></td></tr></tbody></table><p>&nbsp;&nbsp;&nbsp;&nbsp; 这也是MySql官方文档给出的一个例子。三个事务同时插入一条某个唯一索引属性（上面的uuid）相同的数据，其中某个事务先一步插入，其他两个事务会阻塞等待，然后先一步插入的事务回滚，其他两个事务出现死锁，其中某个事务会被回滚。官方文档还提到了另外一种类似的情况，具体可以参考<a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-locks-set.html" target="_blank" rel="noopener"><br>Locks Set by Different SQL Statements in InnoDB</a><br>&nbsp;&nbsp;&nbsp;&nbsp; 这种死锁的原因是，<code>INSERT</code>的时候，会对唯一索引进行Duplicate Key判断，如果唯一键冲突，则会加共享锁等待，也就是T3时候的事务二和事务三，都会获得共享锁。T4时，事务一回滚，事务二和事务三都会申请升级排他锁，这样就造成类似死锁案例二的情况，形成死锁了</p><h5 id="死锁例五"><a href="#死锁例五" class="headerlink" title="死锁例五"></a>死锁例五</h5><table><thead><tr><th>语句顺序\事务</th><th>事务一</th><th>事务二 </th></tr></thead><tbody><tr><td>T1</td><td>begin;</td><td>begin;</td></tr><tr><td>T2(死锁发生)</td><td>update student set age = age + 1 where name = ‘jack’;</td><td>update student set name = ‘bob’ where id &gt; 100;</td></tr></tbody></table><p>&nbsp;&nbsp;&nbsp;&nbsp; 这个例子引用自<a href="http://mysql.taobao.org/monthly/2016/01/01/" target="_blank" rel="noopener">淘宝数据库内核月报-InnoDB 事务锁系统简介</a>，这个地方的死锁我没有试出来，需要在高并发环境才可能出现，理论上可能会出现。虽然只是两个很简单的更新语句，但是事务一的加锁顺序是，先锁二级索引<code>name_index</code>，再锁聚集索引，事务二的加锁顺序是，先锁聚集索引，再锁二级索引<code>name_index</code>，不同的加锁顺序在并发时可能导致死锁</p><h5 id="死锁小结"><a href="#死锁小结" class="headerlink" title="死锁小结"></a>死锁小结</h5><p>&nbsp;&nbsp;&nbsp;&nbsp;使用<code>SHOW ENGINE INNODB STATUS</code>语句可以看到最近一次的死锁信息，在调试的时候很有帮助。<br>&nbsp;&nbsp;&nbsp;&nbsp;出现死锁后某个事务会回滚，其他事务成功，上层业务会捕获到死锁错误，再重试一般会成功，如果出现大量锁重试，则说明哪里出了问题，写代码的时候可以注意以下几点可以减少死锁出现的概率：</p><ul><li>类似的业务逻辑尽量以固定的顺序访问表和行</li><li>如果业务允许，大事务拆小，大事务持有锁的时间更长，更容易出现死锁</li><li>为表添加合理的索引，可以看到可重复读级别下，如果不走索引（非ICP的查询）将会为表的每一行记录加锁</li><li>尽量少用for share或者for update语句，虽然看起来只是在一行记录上加锁，但是由于间隙锁和临键锁的存在，锁住的可能不止是一行记录</li><li>提前申请足够强度的锁，不要先用for share锁住行，后面再update，很容易死锁</li></ul><h3 id="锁的内部表示"><a href="#锁的内部表示" class="headerlink" title="锁的内部表示"></a>锁的内部表示</h3><p>在<code>innoDb</code>内部中，用<code>unsigned long</code>类型表示锁的类型，其中不同的位代表锁不同的信息，最低的4位表示lock_mode，中间的4位表示lock_type，其余高位表示record_lock_type，内部使用位操作来设置和判断是否设置了对应的值 ：</p><table><thead><tr><th></th><th>record_lock_type</th><th>lock_type</th><th>lock_mode</th></tr></thead><tbody><tr><td></td></tr></tbody></table><p><code>lock_mode</code>：描述了锁的基本类型，分为以下几种</p><ul><li>LOCK_IS: 意向共享锁</li><li>LOCK_IX: 意向排他锁</li><li>LOCK_S: 共享锁</li><li>LOCK_X: 排他锁</li><li>LOCK_AUTO_INC: 自增锁</li></ul><p>在源码中有一个lock_mode的枚举类型，除了以上还有几个值：LOCK_NONE，用来表示一致性读，LOCK_NUM用来表示lock_mode的数量，LOCK_NONE_UNSET用来复位低8位  </p><p><code>lock_type</code>：占用中间的4位，目前只用到了5位和6位，分别表示表锁（LOCK_TABLE）和 行锁（LOCK_REC）</p><p><code>record_lock_type</code>：对于表锁类型来说都是空的，对于行锁目前值有：</p><ul><li>LOCK_WAIT：表示正在等待锁</li><li>LOCK_ORDINARY：表示临键锁</li><li>LOCK_GAP：表示间隙锁</li><li>LOCK_REC_NOT_GAP：表示记录锁</li><li>LOCK_INSERT_INTENTION：表示插入意向锁</li><li>LOCK_CONV_BY_OTHER：表示锁是由其它事务创建的(比如隐式锁转换)</li></ul><p>以上说的是锁的类型的表示，行锁、表锁类型相关信息都统一到一个字段了。同类型字段一样，行锁、表锁本身在<code>innoDb</code>中也统一用一个结构体来表示<code>lock_t</code>，大体如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">lock_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">trx_t</span>* trx; <span class="comment">// 锁所属的事务</span></span><br><span class="line">    UT_LIST_NODE_T(<span class="keyword">lock_t</span>) trx_locks; <span class="comment">// 事务所持锁的列表</span></span><br><span class="line">    ulint type_mode; <span class="comment">// 锁类型</span></span><br><span class="line">    <span class="keyword">hash_node_t</span> hash; <span class="comment">// 全局锁哈希表对应的节点</span></span><br><span class="line">    <span class="keyword">dict_index_t</span>* index; <span class="comment">// 行锁的行记录索引</span></span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        <span class="keyword">lock_table_t</span>;  <span class="comment">// 表锁 </span></span><br><span class="line">        <span class="keyword">lock_rec_t</span>  rec_lock; <span class="comment">// 行锁</span></span><br><span class="line">    &#125; un_member; <span class="comment">// 锁详情</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>行锁和表锁都用一个<code>lock_t</code>结构来表示，差异部分在一个union结构中表示，里面的type_mode即是上面介绍的锁类型，行锁的结构如下：</p><figure class="highlight thrift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">lock_rec_t</span> </span>&#123;</span><br><span class="line">    ulint space; <span class="comment">// 锁的space id</span></span><br><span class="line">    ulint page_no; <span class="comment">// 锁的page number</span></span><br><span class="line">    ulint n_bits; <span class="comment">// 锁住位置的bitmap</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通过（<code>space</code>,<code>page_no</code>）可以确定锁所在的页，<code>innoDb</code>内部还会有个字段<code>heap_no</code>来表示记录在页上的偏移，也就是说三元组（<code>space</code>,<code>page_no</code>,<code>heap_no</code>）可以唯一的确定一行的位置。在分配<code>lock_rec_t</code>结构的时候，还会为其在最后分配一个大小为n_bits的bitmap，而记录偏移的bit即为<code>heap_no</code>，用来快速判断这页哪些记录加了锁。</p><p><code>innoDb</code>所有的行锁会插入到一个全局hash表（<code>lock_sys</code>-&gt;<code>rec_hash</code>）中，相同(<code>space</code>,<code>page_no</code>)也就是同一页的锁会被Hash到同一个bucket里，通过<code>lock_t</code>-&gt;<code>hash</code>串成链表。</p><p>总结一下，就是同一事务，同一类型的行锁在同一页上会复用同一个锁结构<code>lock_t</code>，用后面的bitmap来具体表示锁哪些行，大大节约了空间。同一页上不同的事物或类型的锁通过链表串起来放在<code>rec_hash</code>的同一个bucket里，利用hash的结构先定位到页，然后遍历同一页上不同的<code>lock_t</code>，就可以得到哪些事物的哪些锁锁住了哪些行，这种设计平衡了时间和空间的效率。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; innoDB锁系统配合MVCC机制一起实现了事务的<code>一致性</code>和<code>隔离性</code>，innoDB中的锁总类繁多，并且和事务隔离级别关系密切，不同语句在不同隔离级别下的加锁情况大有不同，细节尤其多，而了解这些对排查死锁会有很大的帮助。行锁在innoDB中的实现也颇为巧妙，值得学习</p><h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><ul><li><a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-locking.html" target="_blank" rel="noopener">https://dev.mysql.com/doc/refman/8.0/en/innodb-locking.html</a></li><li><a href="http://mysql.taobao.org/monthly/2017/12/02/" target="_blank" rel="noopener">http://mysql.taobao.org/monthly/2017/12/02/</a></li><li><a href="http://mysql.taobao.org/monthly/2016/01/01/" target="_blank" rel="noopener">http://mysql.taobao.org/monthly/2016/01/01/</a></li><li><a href="http://mysql.taobao.org/monthly/2018/05/04/" target="_blank" rel="noopener">http://mysql.taobao.org/monthly/2018/05/04/</a></li><li><a href="https://www.aneasystone.com/archives/2017/12/solving-dead-locks-three.html" target="_blank" rel="noopener">https://www.aneasystone.com/archives/2017/12/solving-dead-locks-three.html</a></li><li><a href="https://www.cnblogs.com/jojop/p/13982679.html" target="_blank" rel="noopener">https://www.cnblogs.com/jojop/p/13982679.html</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 最近做了一次和&lt;code&gt;MySQL（innoDB）&lt;/code&gt;锁有关的技术分享，记录一下&lt;br&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 为了能尽量准确简洁的描述innoDB的锁机制，看了好多的文章，由于inno
      
    
    </summary>
    
    
      <category term="数据库" scheme="https://schecterdamien.github.io/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title>PostgreSQL的事务隔离和MVCC</title>
    <link href="https://schecterdamien.github.io/2019/04/25/pg-mvcc/"/>
    <id>https://schecterdamien.github.io/2019/04/25/pg-mvcc/</id>
    <published>2019-04-24T16:36:43.000Z</published>
    <updated>2021-03-08T07:43:35.193Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp; 因为之前遇到的并发问题，所以又把pg的事务隔离和mvcc实现温习了一遍，稍微整理，遂有此文。   </p><h3 id="数据库并发问题"><a href="#数据库并发问题" class="headerlink" title="数据库并发问题"></a>数据库并发问题</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 说到事务隔离，得先说说数据库可能产生的几种并发问题：  </p><ul><li>脏读（Dirty Read）：一个事务a读到了事务b更新未提交的数据，b回滚了，a读到的数据就是脏数据   </li><li>不可重复读（NonRepeatable Read）：事务a多次读取同一个数据，事务b在a多次读取的过程中，对这条数据做出了更改并提交，导致事务a在多次读取结果不一致</li><li>幻读（Phantom Read）：事务a在批量更新一个表，事务b这个时候插入了一条不同的记录，导致事务a在改完后发现还有一条记录没有改，就像幻觉一样   </li></ul><p>&nbsp;&nbsp;&nbsp;&nbsp; 不可重复读和幻读很容易混淆，其实幻读是不可重复读的一种特殊情况，只不过不可重复读侧重于<strong>修改</strong>，幻读侧重于<strong>新增或删除</strong>，解决不可重复读的问题只需要锁住满足条件的行，解决幻读需要提高事务的隔离级别，但与此同时，事务的隔离级别越高，并发能力也就越低。所以，所以还需要权衡。</p><h3 id="事务隔离级别"><a href="#事务隔离级别" class="headerlink" title="事务隔离级别"></a>事务隔离级别</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 为了有效保证并发读取数据的正确性，提出的事务隔离级别：      </p><ul><li>未提交读（Read Uncommitted）：允许脏读，也就是可能读取到其他事务中未提交的修改   </li><li>提交读(Read Committed)：只能读取到其他事务已经提交的数据。PostgreSQL、Oracle等多数数据库默认都是该级别</li><li>可重复读(Repeated Read)：可重复读。在同一个事务内的查询都是事务开始时刻一致的，InnoDB默认级别。 </li><li>串行读(Serializable)：完全串行化的读，每次读都需要获得表级共享锁，读写相互都会阻塞  </li></ul><p>&nbsp;&nbsp;&nbsp;&nbsp; 在SQL标准中，定义了不同隔离级别会出现的并发问题：</p><table><thead><tr><th></th><th>隔离级别</th><th>脏读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td></td><td>未提交读</td><td>可能</td><td>可能</td><td>可能</td><td></td></tr><tr><td></td><td>已提交读</td><td>不可能</td><td>可能</td><td>可能</td><td></td></tr><tr><td></td><td>可重复读</td><td>不可能</td><td>不可能</td><td>可能</td><td></td></tr><tr><td></td><td>串行读</td><td>不可能</td><td>不可能</td><td>不可能</td><td></td></tr></tbody></table><p>&nbsp;&nbsp;&nbsp;&nbsp; 注意，这是ANSI SQL标准中的定义，但是具体到数据库的实现可能不一样，只会更严格。比如Read Uncommitted这种隔离级别，其实在实际使用中是没有意义的，设置这种级别还不如用Nosql，所以在pg 中其实Read Uncommitted和Read Committed是一样的。<br>&nbsp;&nbsp;&nbsp;&nbsp; 关于Repeated Read，因为在同一个事务内查询到的数据都是在开启事务后第一次查询时候的快照，可知在业务层开启事务后，事务内所有的乐观锁机制都将失效，毕竟都读不到其他事务的提交了，一定会造成写冲突。除此之外，看了不少的讲pg的事务隔离的文章，都是说pg的Repeated Read不会出现幻读…然后实际上，自测发现并不能完全避免。因为幻读其实分为好多种，在《A Critique of ANSI SQL Isolation Levels》论文就定义了好多种的幻读。可以说，pg的Repeated Read基于其MVCC的实现可以避免一部分，但是是无法完全避免的。<br>&nbsp;&nbsp;&nbsp;&nbsp; 关于Serializable，pg在9.1之前，是没有Repeated Read这个隔离级别，只有Serializable。9.1之后，才把之前的Serializable重命名为Repeated Read，然后加上一个更为严格的Serializable隔离级别。在这两个隔离级别下，如果两个不同事务中同时修改一条记录都会导致其中某个事物写失败，所以在应用层需要有重试机制。<br>&nbsp;&nbsp;&nbsp;&nbsp; 关于两者的不同，应该是说Serializable这种隔离级别可以解决更多的幻读问题，Serializable使用谓词锁（Predicate Lock）来防止这些幻读，意思是如果一个事务T1正在执行一个查询，该查询的的WHERE子句存在一个条件表达式E1，同时这个查询下面还有其他更新或者插入操作，那么另外一个事务T2 就不能插入或删除任何满足E1的数据行。比如之前遇到的并发问题，其实可以简单表达为(伪代码)：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> Binding.objects.filter(sku_code=<span class="string">'test'</span>).all():</span><br><span class="line"><span class="comment"># 这里只有用all()才能触发pg的谓词锁，first()，exist()都不行</span></span><br><span class="line">Binding.objects.create(sku_code=<span class="string">'test'</span>)</span><br></pre></td></tr></table></figure></p><p>&nbsp;&nbsp;&nbsp;&nbsp; 如果是在Repeated Read下，可能就重复创建两条sku_code=’test’的Binding了，但是在Serializable里面因为存在读写依赖，并发两条事务中肯定会又一条会失败，会提示’could not serialize access due to read/write dependencies among transactions’错误</p><h3 id="MVCC常用实现方法"><a href="#MVCC常用实现方法" class="headerlink" title="MVCC常用实现方法"></a>MVCC常用实现方法</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 对于以上的事物隔离级别数据库应该怎么来实现呢。早期有通过用复杂的锁来实现的，最终也只是实现一部分，无法避免所有的并发问题，而且用锁来实现，会影响数据库的并发性能。对如今大部分的主流数据库，都是使用 MVCC(Multi-Version Concurrency Control)多版本并发控制来实现，达到并发并能和隔离性的完美平衡。<br>&nbsp;&nbsp;&nbsp;&nbsp; MVCC的基本思想是写数据时，旧的数据作为旧版本并不删除，并发的读还能读到旧版本的数据，这样读和写就可以并发了。一般MVCC有2种实现方法：</p><ul><li>写新数据时，把旧数据转移到一个单独的地方，如回滚段(undo log)中，其他人读数据时，从回滚段中把旧的数据读出来，如Oracle数据库和MySQL中的innodb引擎。</li><li>写新数据时，旧数据不删除，而是把新数据插入。PostgreSQL就是使用的这种实现方法。 </li></ul><p>两种方法各有利弊，相对于第一种来说，PostgreSQL的MVCC实现方式优势在于： </p><ul><li>无论事务进行了多少操作，事务回滚可以立即完成  </li><li>数据可以进行很多更新，不必像Oracle和MySQL的Innodb引擎那样需要经常保证回滚段不会被用完，也不会像oracle数据库那样经常遇到“ORA-1555”错误的困扰</li></ul><p>相比InnoDB和Oracle，PostgreSQL的MVCC缺点在于：  </p><ul><li>旧版本的数据需要清理。当然，PostgreSQL 9.x版本中已经增加了自动清理的辅助进程来定期清理（VACCUM）</li><li>旧版本的数据可能会导致查询需要扫描的数据块增多，从而导致查询变慢</li></ul><h3 id="PostgreSQL的MVCC实现"><a href="#PostgreSQL的MVCC实现" class="headerlink" title="PostgreSQL的MVCC实现"></a>PostgreSQL的MVCC实现</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;PG为了实现MVCC，表上面会有一些系统的隐藏字段（InnoDB类似），在每个tuple(其他数据库的Row)上，会有t_xmin，t_xmax，cmin和cmax，ctid，t_infomask这些字段。其中：  </p><ul><li>t_xmin：存储的是产生这个元组的事务ID，可能是insert或者update语句</li><li>t_xmax：存储的是删除或者锁定这个元组的事务ID</li><li>t_cid：包含cmin和cmax两个字段，分别存储创建这个元组的Command ID和删除这个元组的Command ID（为了标志同一个事物内语句的先后顺序）</li><li>t_ctid存储用来记录当前元组或新元组的物理位置<ul><li>由块号和块内偏移组成</li><li>如果这个元组被更新，则该字段指向更新后的新元组</li><li>这个字段指向自己，且后面t_heap中的xmax字段为空，就说明该元组为最新版本</li></ul></li><li>t_infomask存储元组的xmin和xmax事务状态，t_infomask只有16位，所以最多存储16种独立的状态标志，以下是t_infomask每位分别代表的含义：</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_HASNULL        0x0001    <span class="comment">/* has null attribute(s) */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_HASVARWIDTH        0x0002    <span class="comment">/* has variable-width attribute(s) 有可变参数 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_HASEXTERNAL        0x0004    <span class="comment">/* has external stored attribute(s) */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_HASOID        0x0008    <span class="comment">/* has an object-id field */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMAX_KEYSHR_LOCK    0x0010    <span class="comment">/* xmax is a key-shared locker */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_COMBOCID        0x0020    <span class="comment">/* t_cid is a combo cid */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMAX_EXCL_LOCK    0x0040    <span class="comment">/* xmax is exclusive locker */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMAX_LOCK_ONLY    0x0080    <span class="comment">/* xmax, if valid, is only a locker */</span></span></span><br><span class="line"><span class="comment">/* xmax is a shared locker */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMAX_SHR_LOCK  (HEAP_XMAX_EXCL_LOCK | HEAP_XMAX_KEYSHR_LOCK)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_LOCK_MASK    (HEAP_XMAX_SHR_LOCK | HEAP_XMAX_EXCL_LOCK | \</span></span><br><span class="line">                         HEAP_XMAX_KEYSHR_LOCK)</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMIN_COMMITTED    0x0100    <span class="comment">/* t_xmin committed 即xmin已经提交*/</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMIN_INVALID        0x0200    <span class="comment">/* t_xmin invalid/aborted */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMIN_FROZEN        (HEAP_XMIN_COMMITTED|HEAP_XMIN_INVALID)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMAX_COMMITTED    0x0400    <span class="comment">/* t_xmax committed即xmax已经提交*/</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMAX_INVALID        0x0800    <span class="comment">/* t_xmax invalid/aborted */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XMAX_IS_MULTI        0x1000    <span class="comment">/* t_xmax is a MultiXactId */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_UPDATED        0x2000    <span class="comment">/* this is UPDATEd version of row */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_MOVED_OFF        0x4000    <span class="comment">/* moved to another place by pre-9.0                    * VACUUM FULL; kept for binary                     * upgrade support */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_MOVED_IN        0x8000    <span class="comment">/* moved from another place by pre-9.0                * VACUUM FULL; kept for binary                  * upgrade support */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_MOVED (HEAP_MOVED_OFF | HEAP_MOVED_IN)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEAP_XACT_MASK        0xFFF0    <span class="comment">/* visibility-related bits */</span></span></span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;同时，每个事务都有自己的id，事务id是一个32位的无符号整数。有3个特殊的事务id。</p><ul><li>0代表invalid事务号，无效的事务ID</li><li>1代表bootstrap事务号，系统表初始化的事务ID</li><li>2代表frozon事务。frozon transaction id比任何事务都要老</li></ul><p>&nbsp;&nbsp;&nbsp;&nbsp;可用的有效最小事务ID为3，然后开始递增。同时，事务的状态会保存在Commit log里面，简称clog，事务状态有以下4种：0x00：表示事务正在进行，0x01：事务已提交，0x02：事务已回滚，0x03：子事务已提交。<br>&nbsp;&nbsp;&nbsp;&nbsp;这些隐藏字段具体到每个操作的变化：</p><ul><li>插入数据的时候，插入的tuple的t_xmin就是当前的事务id，t_xmax为0，t_ctid保存的是当前tuple的物理位置，t_infomask为HEAP_XMAX_INVALID。如果提交，则t_infomask增加HEAP_XMIN_COMMITTED标志位，如果回滚，则t_infomask增加HEAP_XMIN_INVALID标志位。</li><li>删除数据的时候，删除的tuple的t_xmax为当前事务ID，t_infomask去掉HEAP_XMAX_INVALID，说明此时t_xmax有效。如果提交，t_infomask加上HEAP_XMAX_COMMITTED，删除有效。如果回滚，则又加上HEAP_XMAX_INVALID，说明这个删除事务无效，但是t_xmax还是当前事务id，这里主要基于性能考虑，可以直接考t_infomask来判断事务状态即可，没必要再把t_xmax刷回0。  </li><li>更新数据的时候，会产生一个新的tuple，同时把那个旧tuple的t_xmax改为当前操作的事务ID，t_ctid改为新tuple的t_ctid，同时把旧的t_infomask加上已冻结状态：HEAP_XMIN_FROZEN（HEAP_XMIN_COMMITTED | HEAP_XMIN_INVALID）。新tuple类似insert的结果，不同的是t_infomask多了个HEAP_UPDATED状态，它的含义是这个版本是由update产生的。如下图：<img src="/2019/04/25/pg-mvcc/1.png"> </li></ul><h3 id="可见性规则"><a href="#可见性规则" class="headerlink" title="可见性规则"></a>可见性规则</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 那怎么判断一个tuple当前事务是否可见呢？tuple对于当前事务的可见性受生成它的事务（t_xmin）的状态、更新它的事务（t_xmax）的状态、当前事务的隔离级别和事务快照（Snapshot）共同影响。<br>&nbsp;&nbsp;&nbsp;&nbsp; 通过<code>SELECT txid_current_snapshot()</code>可以查看当前的事务快照，里面主要包含三个字段：xmin、xmax、xip</p><ul><li>xmin：当前未完结（提交或回滚）并活跃的事务中最小的XID</li><li>xmax：所有已完结事务（提交或回滚）中最大的XID，加1后记录在xmax中（比当前所有已提交的事务id都大）</li><li>xip：当前所有的未完结并活跃的事务的数组  </li></ul><img src="/2019/04/25/pg-mvcc/2.png"> <p>&nbsp;&nbsp;&nbsp;&nbsp; 如上图，当前的事务id为315，获取此时的快照，其中xmin为125，xmax为201（200+1），所以此时的snapshot = 125 : 201 : 140。</p><p>&nbsp;&nbsp;&nbsp;&nbsp; 事务ID小于xmin的事务表示已经被完结，其涉及的修改对当前快照可见；事务ID大于或等于xmax的事务表示正在执行，其所做的修改对当前快照不可见。事务ID处在 [xmin, xmax)区间的事务，已经完结的对当前事务可见，否则不可见。具体到其涉及的每个tuple，需要结合活跃事务列表与事务提交日志CLOG，判断其所作的修改对当前快照是否可见：</p><ul><li><p><strong>生成它的事务已提交，且尚未被其他事务修改或锁定的tuple可见</strong>。此时t_xmax = 0，且t_infomask为HEAP_XMIN_COMMITTED | HEAP_XMAX_INVALID</p></li><li><p><strong>修改它的事务已中止的tuple可见</strong>。此时t_xmax != 0，且t_infomask为HEAP_XMAX_INVALID。</p></li><li><p><strong>被其他事务正在修改的已冻结的tuple可见</strong>。此时t_xmax != 0，且xmax对应的事务状态为进行中，且t_infomask为HEAP_XMIN_FROZEN（HEAP_XMIN_COMMITTED | HEAP_XMIN_INVALID）。</p></li><li><p><strong>由当前事务生成的，且尚未被当前事务修改的tuple可见</strong>。此时t_xmin为当前事务，t_xmax=0，t_infomask为HEAP_XMAX_INVALID</p></li><li><p><strong>已经被其他事务修改，且该事务已提交的tuple不可见</strong>。此时t_xmax != 0，t_xmax不为当前事务id，且t_infomask为HEAP_XMAX_COMMITTED</p></li><li><p><strong>生成它的事务已中止的tuple不可见</strong>。即t_xmin不为当前事务id，且t_infomask为HEAP_XMIN_INVALID，且没有HEAP_XMIN_COMMITTED。</p></li><li><p><strong>被当前事务修改的tuple不可见</strong>。此时t_xmax为当前事务。</p></li></ul><p>&nbsp;&nbsp;&nbsp;&nbsp;以上列出的判断规则其实主要还是为了保证只看到已经完结的事务（避免脏读）。因为没有看源码中的方法，来源都是实际实验中测出来的或者其他文章的总结，不能保证一定全，只是用来帮助理解，更深入的可以查看源码或者《PostgreSQL数据库内核分析》<br>&nbsp;&nbsp;&nbsp;&nbsp;因为更新的时候，会把原版本的t_ctid指向新的tuple，这样就从旧到新形成了一条版本链（InnoDB类似，不过是从新到旧）。不过需要注意的是，更新操作可能会使表的每个索引也产生新版本的索引记录，即对一条元组的每个版本都有对应版本的索引记录，即对一条元组的每个版本都有对应版本的索引记录。这样带来的问题就是浪费了存储空间，旧版本占用的空间只有在进行VACCUM时才能被回收，增加了数据库的负担。为了减缓更新索引带来的影响，8.3之后开始使用HOT机制。定义符合下面条件的为HOT元组：</p><ul><li>索引属性没有被修改</li><li>更新的元组新旧版本在同一个page中，其中新的被称为HOT元组</li></ul><p>&nbsp;&nbsp;&nbsp;&nbsp;更新一条HOT元组不需要引入新版本的索引，当通过索引获取元组时首先会找到最旧的元组，然后通过元组的版本链找到HOT元组。这样HOT机制让拥有相同索引键值的不同版本元组共用一个索引记录，减少了索引的不必要更新。</p><h3 id="隔离级别的实现"><a href="#隔离级别的实现" class="headerlink" title="隔离级别的实现"></a>隔离级别的实现</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;了解了MVCC原理，那PostgreSQL是怎么实现事务隔离级别呢，在这点上，其实InnoDB也类似，都是根据获取快照的时机不同，实现不同的隔离级别：</p><ul><li>读未提交/读已提交：事务重每个query都会获取最新的快照</li><li>重复读：事务中只有开始的第一个query会获取快照，后面的都以这个快照为基础，不再重复获取快照</li><li>串行读：使用锁系统来实现（SSI）</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;对比InnoDB，PG的MVCC实现方法有利有弊。其中最直接的问题就是表膨胀，为了解决这个问题引入了AutoVacuum自动清理辅助进程，将MVCC带来的垃圾数据定期清理。PG的回滚可以立即完成，但是InnoDB需要回退undo log中的数据。另一方面判断可见性PG更复杂，开销更大，pg还需要访问clog来判断事务状态，底层也因为采用了堆存储数据而不是聚集索引来组织数据导致VACUUM回收的时候可能会产生碎片。不过对比Serializable级别的实现，PG貌似更先进些，这部分还了解的不是很清楚，留个坑后面再详解</p><h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><ul><li><a href="https://peter.grman.at/postgres-repeatable-read-vs-serializable/" target="_blank" rel="noopener">https://peter.grman.at/postgres-repeatable-read-vs-serializable/</a></li><li><a href="https://www.wanghengbin.com/2018/03/29/postgresql-mvcc-lock/" target="_blank" rel="noopener">https://www.wanghengbin.com/2018/03/29/postgresql-mvcc-lock/</a></li><li><a href="http://www.cnblogs.com/dhcn/p/7120895.html" target="_blank" rel="noopener">http://www.cnblogs.com/dhcn/p/7120895.html</a></li><li><a href="http://blog.itpub.net/6906/viewspace-2562652/" target="_blank" rel="noopener">http://blog.itpub.net/6906/viewspace-2562652/</a></li><li><a href="https://tech.meituan.com/2014/08/20/innodb-lock.html" target="_blank" rel="noopener">https://tech.meituan.com/2014/08/20/innodb-lock.html</a></li><li><a href="http://mysql.taobao.org/monthly/2017/10/01/" target="_blank" rel="noopener">http://mysql.taobao.org/monthly/2017/10/01/</a></li><li><a href="https://blog.csdn.net/collin1211/article/details/6024691" target="_blank" rel="noopener">https://blog.csdn.net/collin1211/article/details/6024691</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 因为之前遇到的并发问题，所以又把pg的事务隔离和mvcc实现温习了一遍，稍微整理，遂有此文。   &lt;/p&gt;
&lt;h3 id=&quot;数据库并发问题&quot;&gt;&lt;a href=&quot;#数据库并发问题&quot; class=&quot;headerlink&quot; tit
      
    
    </summary>
    
    
      <category term="数据库" scheme="https://schecterdamien.github.io/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title>记一次并发问题的排查</title>
    <link href="https://schecterdamien.github.io/2019/04/24/concurrency-problem/"/>
    <id>https://schecterdamien.github.io/2019/04/24/concurrency-problem/</id>
    <published>2019-04-24T13:31:41.000Z</published>
    <updated>2021-03-08T07:43:35.192Z</updated>
    
    <content type="html"><![CDATA[<h3 id="起源"><a href="#起源" class="headerlink" title="起源"></a>起源</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 事情的起源是这样的，Binding是一张多对多的表，主要有sku_code，settlement_sn，enable这几个字段，但是逻辑上sku_code，settlemen_sn，enable=True的在表中应该是唯一的。因为enable=False可能有多条，所以不能在数据库上加联合唯一的索引，但是代码里面有判断。然后有一天测试发现出现两条记录的sku_code，settlemen_sn相同且enable都为True，然后看代码，创建Binding的代码大致是这样的</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_binding</span><span class="params">(sku_code, settlement_sn)</span>：</span></span><br><span class="line"><span class="function">    <span class="title">if</span> <span class="title">not</span> <span class="title">Binding</span>.<span class="title">objects</span>.<span class="title">filter</span><span class="params">(sku_code=sku_code, settlement_sn=settlement_sn, enable=True)</span>.<span class="title">exist</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="keyword">raise</span> OperationError(<span class="string">'binding_created'</span>)</span><br><span class="line">    <span class="comment"># 一系列有些耗时的校验</span></span><br><span class="line">    do_some_check()</span><br><span class="line">    <span class="keyword">return</span> Binding.objects.create(settlement_sn=settlement_sn, sku_code=sku_code, enable=<span class="keyword">True</span>)</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp; 先判断这样的Binding是否存在，存在的话就直接抛异常，不存在的话再进行其他的校验，最后生成对应的Binding。一般情况下没问题，但是如果并发请求的sku_code和settlement_sn都相同，接到请求的几个worker同时从数据库查询发现目标binding不存在，做出可以创建的判断（因为下面存在一些耗时的校验让这种情况的概率变得很高），然后就创建了sku_code和settlement_sn都相同的记录。<br>&nbsp;&nbsp;&nbsp;&nbsp; 因为是公司内部使用的财务规则系统，也就几个人在用，所以做的时候没怎么考虑并发问题。最后发现其实是前端没有做按钮的防重点，用户连续在按钮上点了两下，造成了这个问题。<br>&nbsp;&nbsp;&nbsp;&nbsp; 我们用的是pg（等下会说为什么强调是pg），这里其实是发生了<code>幻读</code>。那改事务隔离级别可以解决吗，<code>可重复读</code>和<code>串行化</code>不是能解决<code>幻读</code>吗。。。。。肯定不行，一言不合就改数据库事务隔离级别肯定是不行的，而且改了也没法解决这种<code>幻读</code>问题(后来才知道其实<code>串行化</code>可以，但需要加上失败重试的逻辑)。那加锁吧！当时也没多想，迷迷糊糊的在判断上加个锁：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_binding</span><span class="params">(sku_code, settlement_sn)</span>：</span></span><br><span class="line"><span class="function">    <span class="title">if</span> <span class="title">not</span> <span class="title">Binding</span>.<span class="title">objects</span>.<span class="title">filter</span><span class="params">(sku_code=sku_code, settlement_sn=settlement_sn, enable=True)</span>.<span class="title">select_for_update</span>).<span class="title">exist</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="keyword">raise</span> OperationError(<span class="string">'binding_created'</span>)</span><br><span class="line">    .....</span><br></pre></td></tr></table></figure></p><p>&nbsp;&nbsp;&nbsp;&nbsp; 查询上加上select_for_update()，阻塞住并发的读，完美！发到测试环境，一看，并不行…还是会生成重，再回头看才明白这个锁有问题。首先，django的<code>select_for_update</code>加上exist()其实是不会生成’select … for update’语句的，然后就算生成了，但是因为没有这样的数据，所以没有row可以被锁，这个锁其实没有任何作用。  </p><h3 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 那怎么解决呢，分布式锁肯定是可以的，给判断的代码块加上锁，这样创建binding的请求就变成串行的了，如果对并发没要求，比如我们这种场景为了防止重点，可以使用这种办法。代码块就变成这样了：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_binding</span><span class="params">(sku_code, settlement_sn)</span>：</span></span><br><span class="line"><span class="function">    # 获取分布式锁</span></span><br><span class="line"><span class="function">    <span class="title">get_lock</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">    <span class="title">if</span> <span class="title">not</span> <span class="title">Binding</span>.<span class="title">objects</span>.<span class="title">filter</span><span class="params">(sku_code=sku_code, settlement_sn=settlement_sn, enable=True)</span>.<span class="title">exist</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="keyword">raise</span> OperationError(<span class="string">'binding_created'</span>)</span><br><span class="line">    <span class="comment"># 一系列有些耗时的校验</span></span><br><span class="line">    do_some_check()</span><br><span class="line">    result = Binding.objects.create(settlement_sn=settlement_sn, sku_code=sku_code, enable=<span class="keyword">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 释放分布式锁</span></span><br><span class="line">    release_lock()</span><br><span class="line">    <span class="keyword">return</span> result</span><br></pre></td></tr></table></figure></p><p>&nbsp;&nbsp;&nbsp;&nbsp; 除此之外有没有别的办法呢，其实我们就是想锁住一个“不存在”的一行，google了一下，搜到一篇文章：<a href="https://rosscoded.com/blog/2018/05/02/locking-phantom-postgresql/" target="_blank" rel="noopener">https://rosscoded.com/blog/2018/05/02/locking-phantom-postgresql/</a>，这里面在pg的数据库级别给出了解决办法。首先可以使用<code>ON CONFLICT</code>语句，其次还可以使用<code>advisory locks</code>，劝告锁，这是pg独有的一种锁，思路挺有意思，不过这是库级别的锁，用多了也不好。但是这些都得在代码里面写sql，最后我们还是选择了基于redis的简单的分布式锁来解决。<br>&nbsp;&nbsp;&nbsp;&nbsp; 回到之前说的，为啥强调是pg呢，因为后来试了一下，在mysql里面，其实是可以给不存在的数据加锁的，mysql里面这个叫<code>gap lock</code>，间隙锁，专门用来解决<code>幻读</code>问题。比如直接<code>select * from binding where sku_code = &quot;123&quot; for update</code>其实会同时加<code>record Lock</code>和<code>gap lock</code>，把已经存在的<code>sku_code=123</code>的行给锁住，同时还会锁住不存在的这些目标行，就是这时其他事务其实是没法写<code>sku_code=123</code>的数据的。mysql只有在<code>可重复读</code>以上的隔离级别才会自动加<code>gap lock</code>。<br>&nbsp;&nbsp;&nbsp;&nbsp; 之后有空再详细写写上面的几种锁。</p><h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><ul><li><a href="https://rosscoded.com/blog/2018/05/02/locking-phantom-postgresql/" target="_blank" rel="noopener">https://rosscoded.com/blog/2018/05/02/locking-phantom-postgresql/</a></li><li><a href="https://ningyu1.github.io/site/post/50-mysql-gap-lock/" target="_blank" rel="noopener">https://ningyu1.github.io/site/post/50-mysql-gap-lock/</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h3 id=&quot;起源&quot;&gt;&lt;a href=&quot;#起源&quot; class=&quot;headerlink&quot; title=&quot;起源&quot;&gt;&lt;/a&gt;起源&lt;/h3&gt;&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 事情的起源是这样的，Binding是一张多对多的表，主要有sku_code，settlem
      
    
    </summary>
    
    
      <category term="python" scheme="https://schecterdamien.github.io/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>Cuckoo Filter</title>
    <link href="https://schecterdamien.github.io/2019/04/17/cuckoo-filter/"/>
    <id>https://schecterdamien.github.io/2019/04/17/cuckoo-filter/</id>
    <published>2019-04-17T15:34:49.000Z</published>
    <updated>2021-03-08T07:43:35.189Z</updated>
    
    <content type="html"><![CDATA[<h3 id="比Bloom-filter更好的过滤器"><a href="#比Bloom-filter更好的过滤器" class="headerlink" title="比Bloom-filter更好的过滤器"></a>比Bloom-filter更好的过滤器</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 我们经常会遇到一个需求，就是判断一个元素是否在某个集合里面，首先想到的是维护一个HashMap（比如python中的字典），但是存储容量占比高，考虑到负载因子的存在，通常空间是不能被用满的，而一旦你的值很多例如上亿的时候，那 HashMap 占据的内存大小就变得很可观了，还需要存储对象的key（防止碰撞后没法确定元素），内存可能会装不下，我们需要一个空间效率更高的数据结构，这个时候我们一般会想到Bloom-filter。Bloom-filter可以看作k个hash函数+Bitmap，原理是通过k个hash函数将元素映射到Bitmap的k个位置，因为Bitmap只用1个bit就可以存储一个值，所以空间效率非常高，关于Bloom-filter，网络上已经有很多介绍的文章，就不赘述了。对于判断一个数据是否在某个海量数据的集合里面，Bloom-filter有奇效，比如应对缓存穿透、垃圾邮件的判断等<br>&nbsp;&nbsp;&nbsp;&nbsp; 但是Bloom-filter并不是完美的，比如Bloom-filter不支持删除，一旦需要删除一个元素，就只能重建Bloom-filter，简单的把元素hash后的k个位置置0会造成其他元素的误判（误判一个已经存在的元素为不存在，破坏了Bloom-filter的特性），对于此，有不少的Bloom-filter的变种，比如counting Bloom，d-left counting Bloom，大致的原理是把Bitmap位数组的每一位扩展为一个小的计数器。增加了实现复杂度的同时还降低了空间利用率，那有没有更好的替代方案呢？所以由此引入这次的主角：Cuckoo-Filter</p><h3 id="Cuckoo-Hash"><a href="#Cuckoo-Hash" class="headerlink" title="Cuckoo Hash"></a>Cuckoo Hash</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 介绍Cuckoo-Filter之前得先说说Cuckoo Hash。Cuckoo Hash其实是一种解决hash冲突的策略。<br>&nbsp;&nbsp;&nbsp;&nbsp; Cuckoo是布谷鸟的意思，也就是我们常说的杜鹃。“鸠占鹊巢”里面的“鸠”的就是类似杜鹃这种鸟类。这种鸟有一种即狡猾又贪婪的习性，它不肯自己筑巢，而是把蛋下到别的鸟巢里，而且它的幼鸟又会比别的鸟早出生，布谷幼鸟天生有一种残忍的动作，幼鸟会拼命把未出生的其它鸟蛋挤出窝巢，今后以便独享“养父母”的食物。借助生物学上这一典故，cuckoo hashing处理碰撞的方法，就是把原来占用位置的这个元素踢走，不过被踢出去的元素还要比鸟蛋幸运，因为它还有一个备用位置可以安置，如果备用位置上还有人，再把它踢走，如此往复。直到被踢的次数达到一个上限，才确认哈希表已满，并执行rehash操作，下图是一个Cuckoo Hash的例子：</p><img src="/2019/04/17/cuckoo-filter/1.png"> <p>&nbsp;&nbsp;&nbsp;&nbsp;Cuckoo hash会有多个hash函数（一般是两个），把一个元素x映射到多个位置上，这一点和Bloom-filter很像，但是Cuckoo hash只会在多个位置中选择一个空的位置来存储x（或其他信息，取决于实现）。如果都满了，就随机选一个位置“踢走”那个元素，把那个元素踢到它自己的替代位置上去，如果它的替代位置也满了，则继续踢。图中就是要插入一个x，发现hash后的两个位置都被占了，所以随机选择了6的位置踢走了元素a，a找到了自己的备用位置4发现已经被c占了，接着把c踢走，c最后找到了一个空的位置1，整个插入过程完成。<br>&nbsp;&nbsp;&nbsp;&nbsp; 这样会造成一个问题，就是插入的开销会在元素越来越多的时候变得越来越大，元素可能会一直踢来踢去，甚至出现死循环，比如下图（极端情况）<br><img src="/2019/04/17/cuckoo-filter/2.png"><br>&nbsp;&nbsp;&nbsp;&nbsp; 元素x经过2个hash后的位置都被占了，随机选择一个位置12，把e踢走，e的备用位置被a占了，踢走a，a又踢走了b，b又踢走了c，c踢走了d，d的备用位置是12，于是又把刚插入的x给踢走了……陷入了死循环，所以需要一个上限，达到上限只能重建。此时可以衡量Cuckoo Hash的最大负载因子，负载因子 = 已经存入的元素 / 总的bucket。上图中这种可以看作只有一个slot的bucket，增加slot比如图c，可以大大减少冲突后踢走元素的概率，每个bucket对应一个hash值，相同hash值的元素都存在同一个bucket的不同slot中，除非2个bucket的8个slot都满了，否则不会踢元素。一维数组实现的cuckoo hash的负载因子大小和其他hash策略没什么区别，只有50%，但是多路slot的实现，比如图c中，能达到90%以上，可以说效率很高了。</p><h3 id="Cuckoo-Filter设计"><a href="#Cuckoo-Filter设计" class="headerlink" title="Cuckoo-Filter设计"></a>Cuckoo-Filter设计</h3><p>&nbsp;&nbsp;&nbsp;&nbsp; 本文主要参考了<a href="https://coolshell.cn/articles/17225.html" target="_blank" rel="noopener">酷壳</a>和<a href="https://www.cs.cmu.edu/~dga/papers/cuckoo-conext2014.pdf" target="_blank" rel="noopener">CMU论文</a>的实现，这篇论文首先提出了Cuckoo-Filter，很有参考价值。<br>&nbsp;&nbsp;&nbsp;&nbsp; 论文中在Cuckoo-Filter的设计上比起基本的Cuckoo Hash有不少的优化。作为过滤器，空间肯定是能省就省，所以在每个slot中不会存原本的元素x，而是存的元素x的fingerprint(指纹，记做f)。由于存的是指纹，没有存x本身或者x的备用位置，或者那么在寻找元素的备用位置的时候怎么知道备用位置呢。作者采用了一种比较巧妙的办法，第一个地址用hash(x)来确定，第二个地址用hash(f)和第一个地址做异或来确定，这样不管当前是哪个地址，直接用当前地址和hash(f)做异或就可以得到另外一个地址。同时，插入的时候优先选择空的位置，而不是踢走其他的元素。<br>&nbsp;&nbsp;&nbsp;&nbsp; 刚开始其实还有一个比较疑惑的点，为什么求第二个地址的时候用的是hash(f)来异或呢，而不是直接异或f得到第二个地址，最后在论文中找到了答案(有些词不好翻译就直接贴原文了)：</p><blockquote><p>In addition, the fingerprint is hashed before it is xor-ed with the index of its current bucket to help distribute the items uniformly in the table. If the alternate location were calculated by “i ⊕ fingerprint” without hashing the fingerprint, the items kicked out from nearby buckets would land close to each other in the table, if the size of the fingerprint is small compared to the table size. For example, using 8-bit fingerprints the items kicked out from bucket i will be placed to buckets that are at most 256 buckets away from bucket i, because the xor operation would alter the eight low order bits of the bucket index while the higher order bits would not change. Hashing the fingerprints ensures that these items can be relocated to buckets in an entirely different part of the hash table, hence reducing hash collisions and improving the table utilization.   </p></blockquote><p>&nbsp;&nbsp;&nbsp;&nbsp; 主要是因为fingerprint可能远小于hash(x)，直接异或fingerprint可能导致异或的只有地址1的低位，造成的结果是地址2可能就在地址1的附近，8位的fingerprint下地址1和地址2最大偏移距离只有256，两个地址产生了某种关联性，会加大产生冲突的可能（直接理解是没问题的，但是作者没有给出证明）<br>&nbsp;&nbsp;&nbsp;&nbsp; 除了上面说的这些，作者还做了一个优化来节约空间，称之为”Semi-sorting Buckets”。这种做法的前提是fingerprint在slot中的顺序不影响查询。具体的比如4个slot的bucket，每个slot中保存4位的fingerprint，那么bucket一共是16位。如果我们对4位的fingerprint进行排序，总共有3876种可能（??这个地方没怎么看懂，不知道这个数怎么出来的，留个坑），也就是16位的空间没有完全利用起来，3876&lt;2^12，所以我们可以建立一个12位数值到这部分16位的值的映射。bucket只保存这个映射的key，也就是12位，每次对bucket的处理都相当于经过了一次解码编码的过程，时间上会造成一定的损耗，但是空间利用率更高了，16个bit的bucket节约了4个bit的空间，相当于3/4。<br>&nbsp;&nbsp;&nbsp;&nbsp; 下面会贴出插入、查找、删除的伪代码(来源论文，没有做Semi-sorting的bucket)：  </p><h4 id="nbsp-nbsp-nbsp-nbsp-插入"><a href="#nbsp-nbsp-nbsp-nbsp-插入" class="headerlink" title="&nbsp;&nbsp;&nbsp;&nbsp;插入"></a>&nbsp;&nbsp;&nbsp;&nbsp;插入</h4><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">f = fingerprint(x)</span><br><span class="line">i1 = hash(x)</span><br><span class="line">i2 = i1 xor hash(f)</span><br><span class="line"><span class="keyword">if</span> bucket[i1] <span class="keyword">or</span> bucket[i2] has an empty entry then</span><br><span class="line">    add f to that bucket</span><br><span class="line">    <span class="keyword">return</span> Done</span><br><span class="line">i  = randomly pick i1 <span class="keyword">or</span> i2</span><br><span class="line"><span class="keyword">for</span> n=<span class="number">0</span>;n&lt;MaxNumKicks;++n</span><br><span class="line">    randomly select an entry e <span class="keyword">from</span> bucket[i]</span><br><span class="line">    swap f <span class="keyword">and</span> the fingerprint stored <span class="keyword">in</span> entry e</span><br><span class="line">    i = i xor hash(f)</span><br><span class="line">    <span class="keyword">if</span> bucket[i] has an empty entry then</span><br><span class="line">        add f to bucket[i]</span><br><span class="line">        <span class="keyword">return</span> Done</span><br><span class="line"><span class="keyword">return</span> Failure</span><br></pre></td></tr></table></figure><p>首先计算了元素x的指纹f、两个地址i1和i2，然后查看i1和i2是否有空位，有的话直接插入，返回成功。没有的话随机挑一个踢出去，取踢出去的值赋给f，通过<code>hash(f) xor i</code>找到这个踢出去的元素的备用地址，再看是否被占用，没有的话直接插入，有的话重复上面一个过程，超过最大踢的次数就表明过滤器已经满了需要重建。</p><h4 id="nbsp-nbsp-nbsp-nbsp-查找"><a href="#nbsp-nbsp-nbsp-nbsp-查找" class="headerlink" title="&nbsp;&nbsp;&nbsp;&nbsp;查找"></a>&nbsp;&nbsp;&nbsp;&nbsp;查找</h4><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">f = fingerprint(x)</span><br><span class="line">i1 = hash(x)</span><br><span class="line">i2 = i1 xor hash(f)</span><br><span class="line"><span class="keyword">if</span> bucket[i1] <span class="keyword">or</span> bucket[i2] has f then</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line"><span class="keyword">return</span> <span class="keyword">False</span></span><br></pre></td></tr></table></figure><p>查找的比较简单，计算出了两个地址，如果在两个slot中发现了指纹说明元素存在（有一定误判率，下面会分析）</p><h4 id="nbsp-nbsp-nbsp-nbsp-删除"><a href="#nbsp-nbsp-nbsp-nbsp-删除" class="headerlink" title="&nbsp;&nbsp;&nbsp;&nbsp;删除"></a>&nbsp;&nbsp;&nbsp;&nbsp;删除</h4><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">f = fingerprint(x)</span><br><span class="line">i1 = hash(x)</span><br><span class="line">i2 = i1 xor hash(f)</span><br><span class="line"><span class="keyword">if</span> bucket[i1] <span class="keyword">or</span> bucket[i2] has f then</span><br><span class="line">    remove a copy of f <span class="keyword">from</span> this bucket</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">False</span></span><br></pre></td></tr></table></figure><p>和查找一样，删除也有一定的误判率，如果在被删除元素的备选位置上，有个元素的备选位置是被删除元素的当前位置，并且它的指纹也和被删除元素一样，那么即使删除了当前元素，再找的时候也会产生误判这个元素还存在。</p><h3 id="Cuckoo-Filter相关分析"><a href="#Cuckoo-Filter相关分析" class="headerlink" title="Cuckoo-Filter相关分析"></a>Cuckoo-Filter相关分析</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;Cuckoo-Filter和Bloom-Filter一样，存在着一定的误判率，比如两个元素x、y，如果x、y的fingerprint一样都为f，而且同时<code>hash(x) == hash(y)</code> 或者 <code>hash(x) == hash(y) ^ f</code> 则过滤器根本无法分辨x和y，插入x，再查y发现y也存在，其实查到的是x。这种碰撞发生的概率叫做false positive(假阳率)，就是有可能判断一个并不存在的元素为存在，类似Bloom-Filter。另一方面，对于不存在的元素，肯定能返回不存在的结果。<br>&nbsp;&nbsp;&nbsp;&nbsp;对Cuckoo-Filter来说，可以从这几个方面来衡量它的好坏，一个是空间利用率，一个是插入、查找、和删除的时间开销。<br>&nbsp;&nbsp;&nbsp;&nbsp;在计算空间利用率之前，可以先设置相关的变量。这里我们沿用作者的设定：</p><img src="/2019/04/17/cuckoo-filter/3.png"> <p>&nbsp;&nbsp;&nbsp;&nbsp; ε表示我们期望达到的过滤器的错误率，f表示指纹的长度（bit），α表示负载因子（load factor），b表示每个bucket的slot的数量，m表示过滤器中一共有多少个bucket，n表示我们要插入的item的总数，C表示的平均每个item占用的bit数。我们要计算的就是C，代表的其实就是Cuckoo-filter的空间效率。</p><img src="/2019/04/17/cuckoo-filter/4.png"> <p>&nbsp;&nbsp;&nbsp;&nbsp;那么这个值怎么算呢，平均每个item的大小(bit)=过滤器的总的大小(bit)/插入的item的数目，最后化简得到f/α。也就是说明和指纹长度成正比，和负载因子成反比。说明负载因子越大，空间效率越高，这个好理解。同时指纹长度越小，空间效率越高，那指纹长度能不能无限小呢？当然不行，比如求出了一个地址hash(x)，那备用的地址就只有f ^ 2种可能，指纹长度越小，碰撞的概率就越大，间接的影响了负载因子的大小，作者在论文中表明了这种影响。<br>&nbsp;&nbsp;&nbsp;&nbsp;除此之外，还有一个约束条件，就是我们期望的错误率ε。错误率我们怎么和指纹长度联系起来呢？错误率其实就是把一个没在过滤器中的元素给误判成在存在的概率</p><img src="/2019/04/17/cuckoo-filter/5.png"><p>&nbsp;&nbsp;&nbsp;&nbsp;上图是计算错误率的公式，比如插入x，hash(x)得到第一个bucket的位置，这个bucket和备用bucket一共有2b个元素。和其中某个元素指纹相同的概率为<code>1/2^f</code>，先计算出和2b个元素指纹不碰撞的概率，然后用1减去不碰撞的概率得到就是错误率。</p><img src="/2019/04/17/cuckoo-filter/6.png"><p>&nbsp;&nbsp;&nbsp;&nbsp;代入期望达到的错误率ε，得到指纹长度的下界。这里表明了一个问题，越大的cuckoo-filter为了保持一定的错误率，需要越大的指纹长度，否则错误率将增加。</p><img src="/2019/04/17/cuckoo-filter/7.png"><p>&nbsp;&nbsp;&nbsp;&nbsp;和上面计算空间效率的公式结合，可以得到这个公式，指明了我们期望的错误率越小或每个bucket的slot数目越大则平均每个item的空间损耗的上界越大。</p><img src="/2019/04/17/cuckoo-filter/7.png"><p>&nbsp;&nbsp;&nbsp;&nbsp;查找的时间消耗。查找一个元素，最多会查找2b次，删除也是，可以认定为O(1)的时间复杂度。插入元素的时间复杂渐进为O(1)，在前期bucket没满的时候，直接插入即可，满了就开始踢元素，当负载因子增加的时候，插入的时间将来越久，最终超过最大的踢的次数就只能重建了，时间复杂度的具体证明见原论文。</p><h3 id="Cuckoo-Filter和Bloom-Filter对比"><a href="#Cuckoo-Filter和Bloom-Filter对比" class="headerlink" title="Cuckoo-Filter和Bloom-Filter对比"></a>Cuckoo-Filter和Bloom-Filter对比</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;Cuckoo-Filter对比Bloom-Filter，最主要的就是支持删除，虽然counting Bloom也支持删除，但是空间利用率又远远不如Cuckoo-Filter。在空间利用率上，原论文给出了对比：</p><img src="/2019/04/17/cuckoo-filter/8.png"><p>&nbsp;&nbsp;&nbsp;&nbsp;上面我们讨论过，Cuckoo-Filter的空间效率和我们预定的错误率是有关系的，Bloom-Filter也是这样的，从图中可以看到，当错误率小于3%的时候，semisort-Cuckoo Filter的空间效率是比Bloom-Filter要高的，即使是不是semisort优化的普通Cuckoo，在图中可见的0.1%错误率以内，也是要比Bloom-Filter空间效率要高。而在实际使用中，我们一般不会容忍这么高的错误错误率，所以实际上来说Cuckoo-Filter的空间性能要更好一点。<br>&nbsp;&nbsp;&nbsp;&nbsp;时间性能上，两者的查找都是常量时间复杂度，但是如果数据集过于巨大以至采取了持久化的策略，则Cuckoo-Filter要优于Bloom-Filter，因为最多只需要读取两个bucket，而Bloom-Filter则需要读K次。对于插入，Cuckoo-Filter在负载因子较大的时候因为会踢数据，所以可能会比Bloom-Filter更慢，甚至出现插入失败的情况，这种时候就只能重建了。而Bloom-Filter的插入不会受到负载因子的影响，另外，它还有跟Bloom-Filter共有的一个缺点，就是访问空间地址不连续，通常可以认为是随机的，这样严重破坏了程序局部性，对于Cache流水线来说非常不利。<br>&nbsp;&nbsp;&nbsp;&nbsp;关于两者对比的更多的讨论可以查看这个问题：<a href="https://stackoverflow.com/questions/867099/bloom-filter-or-cuckoo-hashing" target="_blank" rel="noopener">https://stackoverflow.com/questions/867099/bloom-filter-or-cuckoo-hashing</a><br>&nbsp;&nbsp;&nbsp;&nbsp;综上，Cuckoo-Filter是一种很优秀的数据结构，但是目前相关的工程化的应用还不是特别多，不过这也给了我们机会去实现一个更高效的轮子。</p><h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><ul><li><a href="https://www.cs.cmu.edu/~dga/papers/cuckoo-conext2014.pdf" target="_blank" rel="noopener">https://www.cs.cmu.edu/~dga/papers/cuckoo-conext2014.pdf</a></li><li><a href="https://coolshell.cn/articles/17225.html" target="_blank" rel="noopener">https://coolshell.cn/articles/17225.html</a></li><li><a href="https://brilliant.org/wiki/cuckoo-filter/" target="_blank" rel="noopener">https://brilliant.org/wiki/cuckoo-filter/</a></li><li><a href="https://stackoverflow.com/questions/867099/bloom-filter-or-cuckoo-hashing" target="_blank" rel="noopener">https://stackoverflow.com/questions/867099/bloom-filter-or-cuckoo-hashing</a></li><li><a href="http://legendtkl.com/2017/07/23/about-hash-table/" target="_blank" rel="noopener">http://legendtkl.com/2017/07/23/about-hash-table/</a></li><li><a href="https://www.cnblogs.com/chuxiuhong/p/8215719.html" target="_blank" rel="noopener">https://www.cnblogs.com/chuxiuhong/p/8215719.html</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h3 id=&quot;比Bloom-filter更好的过滤器&quot;&gt;&lt;a href=&quot;#比Bloom-filter更好的过滤器&quot; class=&quot;headerlink&quot; title=&quot;比Bloom-filter更好的过滤器&quot;&gt;&lt;/a&gt;比Bloom-filter更好的过滤器&lt;/h3&gt;&lt;p&gt;&amp;n
      
    
    </summary>
    
    
      <category term="algorithm" scheme="https://schecterdamien.github.io/tags/algorithm/"/>
    
  </entry>
  
  <entry>
    <title>怎么高效的调试python程序(1)</title>
    <link href="https://schecterdamien.github.io/2019/02/24/python-debug/"/>
    <id>https://schecterdamien.github.io/2019/02/24/python-debug/</id>
    <published>2019-02-24T03:47:07.000Z</published>
    <updated>2021-03-08T07:43:35.188Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;这篇文章是受到18年pycon上张翔大神演讲的启发，当时他的演讲的题目是《我的python进程怎么了》，主要是介绍了python进程调试的一些工具和方法，总结的比较全，当时在现场听完感觉受益匪浅，但是一直没去整理，这篇文章大致也是按照他的ppt的大纲来写的。<br>&nbsp;&nbsp;&nbsp;&nbsp;我们代码写完后，运行时经常会遇到这样那样的问题，有一些很容易就能发现，因为代码跑到这里就直接就报错了，抛异常了，一般看日志的报错信息就能发现具体是哪里出了问题。但是还有一部分是没法从日志中看出来的：“为什么我的python进程卡住了”，“为什么我的python进程消耗这么多的内存”，“为什么我的python进程消耗了这么多的cpu”，卡住了是因为死锁了还是阻塞在io上了？消耗内存多了是因为存在内存泄漏还是数据结构设计的不合理？这种时候就需要一系列的工具来排查，工欲善其事，必先利其器。</p><h3 id="print-amp-log"><a href="#print-amp-log" class="headerlink" title="print&amp;log"></a>print&amp;log</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;相信对大部分人来说，print和log是最常用的调试方法了。在感觉有问题的地方，加上print和log来输出一些状态信息和变量，每个python程序员都是这方面的大师。<br>&nbsp;&nbsp;&nbsp;&nbsp;print&amp;log大法简单粗暴，但往往很有效。缺点是需要提前了解你的代码，知道在哪里print&amp;log，而且需要修改后重启你的进程，调试完还要删除调试的代码。</p><h3 id="pdb"><a href="#pdb" class="headerlink" title="pdb"></a>pdb</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;pdb是python自带的一个库，为python程序提供了一种交互的源代码调试功能，主要特性包括设置断点、单步调试、进入函数调试、查看当前代码、查看栈片段、动态改变变量的值等。pdb可以认为是python下的gdb，两者保持了一样的用法。<br>&nbsp;&nbsp;&nbsp;&nbsp;有两种不同的方法使用pdb，一种是直接在命令行指定参数启动pdb，比如直接python -m pdb pdb_test.py 可以进入命令行调试模式，接下来可以输入相应的调试命令进行调试，比如：<code>l</code>是查看当前代码，<code>b</code>是设置断点，<code>n</code>是执行下一行，单步调试。输入<code>h</code>可以查看各命令的使用说明。<br>&nbsp;&nbsp;&nbsp;&nbsp;pdb单步执行太麻烦了，第二种方法是直接在代码中import pdb，直接在代码里需要调试的地方放一个 pdb.set_trace() 设置一个断点，程序运行到这里会暂停并进入pdb调试环境，可以用pdb 变量名查看变量，或者c继续运行。关于pdb更详细的使用方法，可以查看<a href="https://docs.python.org/3/library/pdb.html" target="_blank" rel="noopener">官方文档</a><br>&nbsp;&nbsp;&nbsp;&nbsp;但是实际工作中，很多时候我们都是使用flask或者django等框架来开发，特别是django，很多时候需要在django shell里面进行一系列的调试，这个时候pdb就不好启动了，可以使用<a href="https://github.com/HassenPy/django-pdb" target="_blank" rel="noopener">django-pdb</a>。<br>&nbsp;&nbsp;&nbsp;&nbsp;除了pdb，还有一个第三方的开源的python调试器<strong>Ipdb</strong>，具有语法高亮、tab补全，更友好的输出信息等高级功能，ipdb之于pdb，就相当于IPython之于Python，虽然都是实现相同的功能，但是，在易用性方面做了很多的改进。如果使用ide，比如PyCharm，很多都自带了断点设置，变量查看的功能，使用上更加方便友好。</p><h3 id="sys"><a href="#sys" class="headerlink" title="sys"></a>sys</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;sys模块包括了一组非常实用的服务，内含很多函数方法和变量，用来处理Python运行时的<strong>配置以及资源</strong>，从而可以与当前程序之外的系统环境交互，是python程序用来请求<strong>解释器行为</strong>的接口。<br>&nbsp;&nbsp;&nbsp;&nbsp;很长一段时间里面，都不清楚sys模块的重要性，说这是python标准库中最重要的模块之一也不为过了，后来陆续接触到sys模块的一些黑科技般的方法，才逐渐的了解到它的强大，里面很多的方法涉及到的背景和知识都能另起一篇文章了，所以这里只介绍一部分。<br>&nbsp;&nbsp;&nbsp;&nbsp;<code>sys._getframe([depth])</code>: 返回一个栈帧对象，depth为栈顶部向下的深度，默认为0，返回的是当前的栈帧。   </p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> sys</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>frame = sys._getframe()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>dir(frame)</span><br><span class="line">[<span class="string">'__class__'</span>, <span class="string">'__delattr__'</span>, <span class="string">'__dir__'</span>, <span class="string">'__doc__'</span>, <span class="string">'__eq__'</span>, <span class="string">'__format__'</span>, <span class="string">'__ge__'</span>, </span><br><span class="line"><span class="string">'__getattribute__'</span>, <span class="string">'__gt__'</span>, <span class="string">'__hash__'</span>, <span class="string">'__init__'</span>, <span class="string">'__init_subclass__'</span>, <span class="string">'__le__'</span>, </span><br><span class="line"><span class="string">'__lt__'</span>, <span class="string">'__ne__'</span>, <span class="string">'__new__'</span>, <span class="string">'__reduce__'</span>, <span class="string">'__reduce_ex__'</span>, <span class="string">'__repr__'</span>, </span><br><span class="line"><span class="string">'__setattr__'</span>, <span class="string">'__sizeof__'</span>, <span class="string">'__str__'</span>, <span class="string">'__subclasshook__'</span>, <span class="string">'clear'</span>, <span class="string">'f_back'</span>, </span><br><span class="line"><span class="string">'f_builtins'</span>, <span class="string">'f_code'</span>, <span class="string">'f_globals'</span>, <span class="string">'f_lasti'</span>, <span class="string">'f_lineno'</span>, <span class="string">'f_locals'</span>, <span class="string">'f_trace'</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">assert</span> frame.f_back == <span class="keyword">None</span> </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>dir(frame.f_code)</span><br><span class="line">[<span class="string">'__class__'</span>, <span class="string">'__delattr__'</span>, <span class="string">'__dir__'</span>, <span class="string">'__doc__'</span>, <span class="string">'__eq__'</span>, <span class="string">'__format__'</span>, <span class="string">'__ge__'</span>,</span><br><span class="line"> <span class="string">'__getattribute__'</span>, <span class="string">'__gt__'</span>, <span class="string">'__hash__'</span>, <span class="string">'__init__'</span>, <span class="string">'__init_subclass__'</span>, </span><br><span class="line"> <span class="string">'__le__'</span>, <span class="string">'__lt__'</span>, <span class="string">'__ne__'</span>, <span class="string">'__new__'</span>, <span class="string">'__reduce__'</span>, <span class="string">'__reduce_ex__'</span>, <span class="string">'__repr__'</span>, </span><br><span class="line"> <span class="string">'__setattr__'</span>, <span class="string">'__sizeof__'</span>, <span class="string">'__str__'</span>, <span class="string">'__subclasshook__'</span>, <span class="string">'co_argcount'</span>, </span><br><span class="line"> <span class="string">'co_cellvars'</span>, <span class="string">'co_code'</span>, <span class="string">'co_consts'</span>, <span class="string">'co_filename'</span>, <span class="string">'co_firstlineno'</span>, <span class="string">'co_flags'</span>, </span><br><span class="line"> <span class="string">'co_freevars'</span>, <span class="string">'co_kwonlyargcount'</span>, <span class="string">'co_lnotab'</span>, <span class="string">'co_name'</span>, <span class="string">'co_names'</span>, </span><br><span class="line"> <span class="string">'co_nlocals'</span>, <span class="string">'co_stacksize'</span>, <span class="string">'co_varnames'</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>frame.f_code.co_name</span><br><span class="line"><span class="string">'&lt;module&gt;'</span></span><br></pre></td></tr></table></figure><p><code>frame.f_back</code>返回上一个栈帧对象，也就是调用栈的上一层，这里因为是直接在python shell中使用的，调用栈就一层，所以frame.f_back返回<code>None</code>。其他的，比如f_locals返回当前栈帧的本地变量，f_globals返回全局变量。通过f_code可以获得PyCodeObject。这个对象保存了栈帧引用的代码块的一些信息，比如函数名称co_name，这里因为已经是栈底了，所以<code>frame.f_code.co_name</code>显示的是<code>&lt;module&gt;</code>。所以可以通过这些方法写一个函数，可以获取这个函数调用者的一些信息：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">func</span><span class="params">()</span>:</span></span><br><span class="line">    frame = sys._getframe(<span class="number">1</span>)</span><br><span class="line">    code = frame.f_code</span><br><span class="line">    caller_msg = <span class="string">f'func called by <span class="subst">&#123;code.co_name&#125;</span> in file: <span class="subst">&#123;code.co_filename&#125;</span> <span class="subst">&#123;frame.f_lineno&#125;</span> line'</span></span><br><span class="line">    print(caller_msg)</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;<code>sys._current_frames()</code>: 返回函数调用时，每个线程标识符与顶层堆栈帧的字典映射，这里只有一个线程，所以字典只有一项：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;</span>&gt; sys._current_frames()</span><br><span class="line">&#123;<span class="number">140736139600768</span>: &lt;frame object at <span class="number">0x106484528</span>&gt;&#125;</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;<code>sys.getswitchinterval()</code>：返回解释器的“线程切换间隔”，也就是多线程下一个线程执行多久会被gil切换。<br>&nbsp;&nbsp;&nbsp;&nbsp;<code>sys.getrecursionlimit()</code>: 获取最大递归的层数<br>&nbsp;&nbsp;&nbsp;&nbsp;<code>sys.getrefcount(object)</code>：返回对象的引用计数，执行的时候已经又被引用了一次，所以真实的引用次数应该-1<br>&nbsp;&nbsp;&nbsp;&nbsp;<code>sys.getallocatedblocks()</code>：返回解释器当前分配的内存块数量。主要用于追踪和调试内存泄漏。受解释器内部缓存影响，每次调用返回的值都可能不一样，可以通过调用_clear_type_cache()和gc.collect()方法获取更可预测的结果。<br>&nbsp;&nbsp;&nbsp;&nbsp;这里重点介绍了<code>sys._getframe</code>方法，因为之前就做过一个这样的需求，要在一个log工具类中打印出调用这个类的调用者的信息，所以印象很深刻。但是官方文档不建议通过这种方式获取当前栈帧，官方文档并不保证在Python的所有实现中都存在这个方法，建议使用<code>inspect.currentframe()</code>获取当前栈帧。<br>&nbsp;&nbsp;&nbsp;&nbsp;除了上面这些，sys里面还有很多强大的工具，更详细的可以看看<a href="https://docs.python.org/3.6/library/sys.html" target="_blank" rel="noopener">官方文档</a></p><h3 id="profile"><a href="#profile" class="headerlink" title="profile"></a>profile</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;如果我们的进程有大量的计算，又没有对应的输出信息，可能会让我们误以为卡住了，又或者我们需要对性能进行调优，我们需要一些工具来进行性能分析。python官方库有两个很方便的工具：Profile和cProfile（还有个hotshot，但是已经不维护了，所以忽略）。<br>&nbsp;&nbsp;&nbsp;&nbsp;这两者的使用方法完全一样，不同的是cProfile是c实现的，运行开销比较小，适合分析运行时间比较长的项目，是我们大部分情况下的首选。Profile是一个纯python的模块，运行开销比较大，如果想扩展其他的功能的话，可以继承这个模块。<br>&nbsp;&nbsp;&nbsp;&nbsp;提到cProfile，就不得不提pstats，这个模块是用来处理cProfile的分析结果。有两种方法使用cProfile，一种是在命令行里使用<code>python3 -m cProfile profile_test.py</code>，可以得到整个profile_test.py程序运行的分析结果，或者直接在程序内使用，举个栗子:  </p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> cProfile <span class="keyword">import</span> Profile</span><br><span class="line"><span class="keyword">import</span> pstats</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> foo2() * <span class="number">3</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> foo3() * <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo3</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># "this call tree seems ugly, but it always happen"</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">flat_foo</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span> * <span class="number">2</span> * <span class="number">3</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">flat_foo2</span><span class="params">()</span>:</span></span><br><span class="line">    a = <span class="number">1</span></span><br><span class="line">    b = <span class="number">2</span></span><br><span class="line">    c = <span class="number">3</span></span><br><span class="line">    <span class="keyword">return</span> a * b * c</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculate1</span><span class="params">()</span>:</span></span><br><span class="line">    sum = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10000</span>):</span><br><span class="line">        sum += [i * i * i]</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculate2</span><span class="params">()</span>:</span></span><br><span class="line">    sum = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10000</span>):</span><br><span class="line">        sum = sum + [i * i * i]</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">100000</span>):</span><br><span class="line">        <span class="keyword">if</span> i % <span class="number">10000</span> == <span class="number">0</span>:</span><br><span class="line">            calculate1()</span><br><span class="line">        <span class="keyword">elif</span> i % <span class="number">10000</span> == <span class="number">1</span>:</span><br><span class="line">            calculate2()</span><br><span class="line">        <span class="keyword">elif</span> i % <span class="number">3</span> == <span class="number">1</span>:</span><br><span class="line">            flat_foo()</span><br><span class="line">        <span class="keyword">elif</span> i % <span class="number">3</span> == <span class="number">2</span>:</span><br><span class="line">            flat_foo2()</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            foo1()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    prof = Profile()</span><br><span class="line">    prof.runcall(main)</span><br><span class="line"></span><br><span class="line">    ps = pstats.Stats(prof)</span><br><span class="line">    ps.strip_dirs().sort_stats(<span class="string">"tottime"</span>).print_stats()</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;脚本引入Profile类，分析main的执行情况，收集到性能分析的数据后，通过pstats的Stats来进行排序并输出到终端：<br><img src="/2019/02/24/python-debug/1.png"><br>&nbsp;&nbsp;&nbsp;&nbsp;对于上面的的输出，每一个字段意义如下：  </p><ul><li>ncalls 函数总的调用次数    </li><li>tottime 函数内部（不包括子函数）的占用时间   </li><li>percall（第一个） tottime/ncalls   </li><li>cumtime 函数包括子函数所占用的时间</li><li>percall（第二个）cumtime/ncalls. </li><li>filename:lineno(function)  文件：行号（函数） </li></ul><p>&nbsp;&nbsp;&nbsp;&nbsp;分析下这个结果，我们会发现一些有意思的事情。执行时间最长的是calculate1，对比下calculate2发现两者的差异只是一个使用<code>+=</code>操作，一个是直接<code>+</code>的操作，因为列表是可变对象，python对可变对象的<code>+=</code>操作是直接在这个可变对象原分配的内存块上进行，而每次<code>+</code>操作的结果都会重新分配一块内存，无形中降低了效率，这部分性能损耗还是挺大的，可以看到速度相差了100多倍。接下里是foo1，foo1其实只是做了一个<code>1*2*3</code>的操作，但是确需要调用3层的函数来计算，flat_foo和flat_foo2只有1层，说明了调用栈频繁的生成和销毁其实是有一定的运行开销的。那为什么flat_foo和flat_foo2的运行时间也不一样呢，因为python解释器会进行一系列优化，比如对于<code>1 * 2 * 3</code>这种表达式会进行常量替换，直接替换成<code>6</code>，这样就不用每次都计算了，但是对于<code>a * b * c</code>解释器就没法进行替换了，因为python是动态语言，没法进行静态分析，abc的值只有在运行时才能确定。<br>&nbsp;&nbsp;&nbsp;&nbsp;除了直接在终端展示，我们还可以保存cProfile分析的结果。可以在运行时指定，比如python3 -m cProfile profile_test.py -o result，或者在程序里面<code>prof.dump_stats(&#39;result&#39;)</code>。保存下来的结果可以用一些图形化的工具进行分析，比如visualpytune、qcachegrind、runsnakerun等。</p><h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;本来打算一口气把演讲中提到的所有调试工具和方法都写完的，但是真正写的时候发现每个工具都能牵扯出很多的东西。而自己水平也有限，不敢随便就下笔写，只能查阅确认后再写，所以很耗费精力，最后还是打算分成几个部分来写了，加油吧</p><h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><p><a><a href="https://www.ibm.com/developerworks/cn/linux/l-cn-pythondebugger/index.html" target="_blank" rel="noopener">https://www.ibm.com/developerworks/cn/linux/l-cn-pythondebugger/index.html</a></a><br><a><a href="https://zhuanlan.zhihu.com/p/25942045" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/25942045</a></a><br><a><a href="https://docs.python.org/3/library/inspect.html" target="_blank" rel="noopener">https://docs.python.org/3/library/inspect.html</a></a><br><a><a href="https://docs.python.org/3.6/library/sys.html" target="_blank" rel="noopener">https://docs.python.org/3.6/library/sys.html</a></a><br><a><a href="http://www.cnblogs.com/xybaby/p/6510941.html" target="_blank" rel="noopener">http://www.cnblogs.com/xybaby/p/6510941.html</a></a><br><a><a href="https://zhuanlan.zhihu.com/p/24495603" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/24495603</a></a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;这篇文章是受到18年pycon上张翔大神演讲的启发，当时他的演讲的题目是《我的python进程怎么了》，主要是介绍了python进程调试的一些工具和方法，总结的比较全，当时在现场听完感觉受益匪浅，但是一直没去整理，这篇文章大致
      
    
    </summary>
    
    
      <category term="python" scheme="https://schecterdamien.github.io/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>unicode到底是个什么鬼</title>
    <link href="https://schecterdamien.github.io/2019/01/24/unicode/"/>
    <id>https://schecterdamien.github.io/2019/01/24/unicode/</id>
    <published>2019-01-24T08:01:06.000Z</published>
    <updated>2021-03-08T07:43:35.187Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;编码问题可以说是python中非常让人头疼的一个问题了，看到知乎上一个哥们说：一旦你走上了编程之路，如果你不把编码问题搞清楚，那么它就像幽灵一般纠缠你整个职业生涯，各种灵异事件会接踵而来。只有充分发挥程序员死磕到底的精神你才有可能彻底摆脱编码问题带来的烦恼。<br>&nbsp;&nbsp;&nbsp;&nbsp;实在不能更同意了。前几天看到一篇文章写的挺不错<a><a href="https://zhuanlan.zhihu.com/p/40834093" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/40834093</a></a>。网上也有很多类似的文章，一般都会说到python3的str的编码是unicode。同时在另一本知名的书上《effective python》有这样一段：</p><blockquote><blockquote><blockquote><p> 把Unicode字符表示为二进制数据（也就是原始8位值）有许多的办法。最常见的编码方式就是UTF-8。但是大家要记住，python3的str实例和python2的unicode实例都没有和特定的二进制编码形式相关联。要把Unicode字符转换成二进制数据，就必须使用encode方法。想要把二进制数据转换成Unicode字符，则必须使用decode方法。  </p></blockquote></blockquote></blockquote><p>&nbsp;&nbsp;&nbsp;&nbsp;看到这里我就很分裂了，那python3的str到底是什么编码格式呢？如果没有和特定的二进制编码形式相关联，那str在内存中怎么表示呢？unicode到底是个什么鬼？</p><h3 id="Unicode"><a href="#Unicode" class="headerlink" title="Unicode"></a>Unicode</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;说到这里，就得去研究下unicode。<br>&nbsp;&nbsp;&nbsp;&nbsp;刚开始没有的unicode的时候，英语国家使用ascii，中国使用gbk，日本使用shift_jis……大家都使用自己的编码协议，大家交流起来就特别麻烦，一不小心就乱码了。假设没有一门可以包容所有语言的编码，同时有n个适用于各国自己语言的编码，那大家需要交流的话就要n*(n-1)种映射协议来转换编码，让大家都能交流，这无疑对互联网来说是场灾难。所以unicode出现了，它对世界上大部分的文字系统进行了整理、编码，使得计算机可以用更为简单的方式来呈现和处理文字。<br>&nbsp;&nbsp;&nbsp;&nbsp;大概来说，Unicode编码系统可分为编码方式和实现方式两个层次。重点来了，Unicode的编码方式和实现方式是不同的，编码方式是指的字符集，也就是指定了每个值和字符的映射关系，比如我可以自己制定一个很简单的字符集类似于:<code>{1: &#39;你&#39;, 2: &#39;好&#39;，……，260: &#39;呢&#39;}</code>，这里指定的是值和字母的映射关系，1代表的就是’你’，2代表的是’好’，260代表的是’呢’。根据编码方式我们可以有不同的实现方式。比如如果是用定长编码来实现，要存储260这个中文字符，我们至少需要两个字节，所以我们可以写死用两个字节来存储，尽管对于前面256个字符来说，第一个字节都是0。如果使用变长编码来实现，可以把前面128个汉字用一个字节表示，留出首位当作标识位，其他的汉字用两个字节表示。还有比如两个字节是大端序还是小端序会影响到第一个字节是高位还是第二个字节是高位等等，这些都是不同的实现方式。<br>&nbsp;&nbsp;&nbsp;&nbsp;所以Unicode我们通常说的是指的其编码方式，就是映射关系。Unicode的实现方式称为Unicode转换格式（Unicode Transformation Format，简称为UTF）。我们用的比较多的有UTF-8，UTF-16，UTF-32等。所以我们说python3中的str的编码方式是unicode这个说法就有问题了，unicode只是一个字符集，没有约定实现，具体的实现还要看是采用UTF-8还是UTF-16还是别的方式，那到底python中采取的是unicode的哪种实现方式呢？</p><h3 id="真相大白"><a href="#真相大白" class="headerlink" title="真相大白"></a>真相大白</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;然而这个问题，在中文网络上几乎没找到相关的答案，看了好多的博客，都只是说python3的str编码是unicode，这句话本身就有问题。最后还是在stackoverflow上找到了答案: <a href="https://stackoverflow.com/questions/1838170/what-is-internal-representation-of-string-in-python-3-x" target="_blank" rel="noopener">What is internal representation of string in Python 3.x</a> 以及 <a href="https://stackoverflow.com/questions/26079392/how-is-unicode-represented-internally-in-python" target="_blank" rel="noopener">How is unicode represented internally in Python?</a>。<br>&nbsp;&nbsp;&nbsp;&nbsp;总结一下，就是python在3.3之前，包括python2，具体的内部unicode采用哪种方式实现其实是编译时的选项。取决于系统的本机字节顺序以及是否选择了UCS2或UCS4。UCS4的话就是采用UTF-32来实现，UCS2的话就采用UTF-16来实现，本机字节顺序决定了是UTF-16 BE还是UTF-16 LE。也就是一旦在编译的时候设置好了，之后python里面unicode的实现都是这种方式。python3.3之后，采用了一种更为灵活的方式来表示，字符串可能是ascii, latin-1, utf-8, utf-16, utf-32等编码形式中的一种，就是说不再是写死一种方式了（这个地方还没完全弄明白，先留个坑），这个主要是pep 393中约定的：<a href="https://www.python.org/dev/peps/pep-0393/" target="_blank" rel="noopener">PEP 393 – Flexible String Representation</a> </p><h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><ul><li><a><a href="https://www.cnblogs.com/malecrab/p/5300503.html" target="_blank" rel="noopener">https://www.cnblogs.com/malecrab/p/5300503.html</a></a></li><li><a><a href="https://zh.wikipedia.org/wiki/Unicode" target="_blank" rel="noopener">https://zh.wikipedia.org/wiki/Unicode</a></a>  </li><li><a><a href="https://zh.wikipedia.org/wiki/通用字符集#Unicode和ISO_10646的关系" target="_blank" rel="noopener">https://zh.wikipedia.org/wiki/通用字符集#Unicode和ISO_10646的关系</a></a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;编码问题可以说是python中非常让人头疼的一个问题了，看到知乎上一个哥们说：一旦你走上了编程之路，如果你不把编码问题搞清楚，那么它就像幽灵一般纠缠你整个职业生涯，各种灵异事件会接踵而来。只有充分发挥程序员死磕到底的精神你才有
      
    
    </summary>
    
    
      <category term="python" scheme="https://schecterdamien.github.io/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>namedtuple第一个参数有什么用？</title>
    <link href="https://schecterdamien.github.io/2018/12/19/namedtuple/"/>
    <id>https://schecterdamien.github.io/2018/12/19/namedtuple/</id>
    <published>2018-12-19T11:36:33.000Z</published>
    <updated>2021-03-08T07:43:35.187Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;namedtuple是python中一个简单实用的数据结构，但是很不爽的是每次使用的时候都需要给第一个参数指定一个字符串，而这个字符串在实际使用中似乎并没有什么作用。比如</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;</span>&gt; from collections import namedtuple</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; Student = namedtuple(<span class="string">'StudentTuple'</span>, [<span class="string">'sex'</span>, <span class="string">'name'</span>, <span class="string">'age'</span>])</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; s = Student(<span class="string">'male'</span>, <span class="string">'callmedad'</span>, <span class="number">22</span>)</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; s.age</span><br><span class="line"><span class="number">22</span></span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;我操作的对象都是Student，而不是StudentTuple，那我为什么还要传一个’StudentTuple’，这个字符串似乎对我没有任何帮助。stackoverflow同样有人提了这个问题：<a href="https://stackoverflow.com/questions/30526115/whats-the-first-argument-of-namedtuple-used-for" target="_blank" rel="noopener">传送门1</a>，<a href="https://stackoverflow.com/questions/30535678/why-do-you-have-to-provide-the-typename-as-the-first-parameter-when-creating-a-n" target="_blank" rel="noopener">传送门2</a>. 看了下回答，然后又看了下namedtuple的源码，总算明白了。<br>&nbsp;&nbsp;&nbsp;&nbsp;接着上面的代码，我们可以看下Student是个啥。  </p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;</span>&gt; import inspect</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; inspect.isclass(Student)</span><br><span class="line">True</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; Student</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">StudentTuple</span>'&gt;</span></span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; Student</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">StudentTuple</span>'&gt;</span></span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; s.__class_<span class="number">_</span></span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">StudentTuple</span>'&gt;</span></span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; type(namedtuple)</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">function</span>'&gt;</span></span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;我们大致知道这几点：1. Student是个类，并且只是一个类的引用，查看s.__class__知道，这个类定义的名字是StudentTuple，就是我们传的第一个参数。2. namedtuple是个方法，但是返回的是一个新的类，也就是说这是一个类的工厂方法。<br>&nbsp;&nbsp;&nbsp;&nbsp;既然是一个生成类的工厂方法，那么肯定就需要知道类的名字，所以这就是第一个参数的意义。然后，这一切是怎么实现的呢？<br>&nbsp;&nbsp;&nbsp;&nbsp;看下namedtuple的源码，其实非常简单粗暴</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">def namedtuple(typename, field_names, *, <span class="attr">verbose=False,</span> <span class="attr">rename=False,</span> <span class="attr">module=None):</span></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="attr">class_definition</span> = _class_template.format(</span><br><span class="line">        <span class="attr">typename</span> = typename,  <span class="comment"># 第一个参数</span></span><br><span class="line">        <span class="attr">field_names</span> = tuple(field_names),  <span class="comment"># 各种检验后的属性列表</span></span><br><span class="line">        <span class="attr">num_fields</span> = len(field_names), </span><br><span class="line">        <span class="attr">arg_list</span> = repr(tuple(field_names)).replace(<span class="string">"'"</span>, <span class="string">""</span>)[<span class="number">1</span>:-<span class="number">1</span>],</span><br><span class="line">        <span class="attr">repr_fmt</span> = ', '.join(_repr_template.format(<span class="attr">name=name)</span></span><br><span class="line">                             for name <span class="keyword">in</span> field_names),</span><br><span class="line">        <span class="attr">field_defs</span> = '\n'.join(_field_template.format(<span class="attr">index=index,</span> <span class="attr">name=name)</span></span><br><span class="line">                               for index, name <span class="keyword">in</span> enumerate(field_names))</span><br><span class="line">    )</span><br><span class="line">   <span class="attr">namespace</span> = dict(<span class="attr">__name__='namedtuple_%s'</span> % typename)</span><br><span class="line">   exec(class_definition, namespace)</span><br><span class="line">   <span class="attr">result</span> = namespace[typename]</span><br><span class="line">   result.<span class="attr">_source</span> = class_definition</span><br><span class="line">   ...</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;核心代码就这几行，主要思路就是填充一个字符串的类的模版，然后动态执行生成一个类。 _class_template大致长这个样(简化后)：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">_class_template = <span class="string">""</span><span class="comment">"\</span></span><br><span class="line">from builtins import property <span class="keyword">as</span> _property, tuple <span class="keyword">as</span> _tuple</span><br><span class="line">from operator import itemgetter <span class="keyword">as</span> _itemgetter</span><br><span class="line">from collections import OrderedDict</span><br><span class="line"></span><br><span class="line">class &#123;typename&#125;(tuple):</span><br><span class="line">    <span class="string">'&#123;typename&#125;(&#123;arg_list&#125;)'</span></span><br><span class="line"></span><br><span class="line">    __slots__ = ()</span><br><span class="line"></span><br><span class="line">    _fields = &#123;field_names!r&#125;</span><br><span class="line"></span><br><span class="line">    def __new__(_cls, &#123;arg_list&#125;):</span><br><span class="line">        <span class="string">'Create new instance of &#123;typename&#125;(&#123;arg_list&#125;)'</span></span><br><span class="line">        <span class="keyword">return</span> _tuple.__new__(_cls, (&#123;arg_list&#125;))</span><br><span class="line"></span><br><span class="line">       def __repr__(self):</span><br><span class="line">        <span class="string">'Return a nicely formatted representation string'</span></span><br><span class="line">        <span class="keyword">return</span> self.__class__.__name__ + <span class="string">'(&#123;repr_fmt&#125;)'</span> % self</span><br><span class="line"></span><br><span class="line">    ....</span><br><span class="line">   </span><br><span class="line">&#123;field_defs&#125;</span><br><span class="line"><span class="string">""</span><span class="comment">"</span></span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;有人说这种方式很”dirty”，因为是用eval来动态执行的，但是这种方法确实是很简单高效，除了这种方式，其实也可以通过type和元类来动态生成一个类，有空再补上</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;namedtuple是python中一个简单实用的数据结构，但是很不爽的是每次使用的时候都需要给第一个参数指定一个字符串，而这个字符串在实际使用中似乎并没有什么作用。比如&lt;/p&gt;
&lt;figure class=&quot;highligh
      
    
    </summary>
    
    
      <category term="python" scheme="https://schecterdamien.github.io/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>io多路复用</title>
    <link href="https://schecterdamien.github.io/2018/12/19/io-multiplexing/"/>
    <id>https://schecterdamien.github.io/2018/12/19/io-multiplexing/</id>
    <published>2018-12-18T17:15:33.000Z</published>
    <updated>2021-03-08T07:43:35.187Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;虽然之前也看过不少关于io多路复用的文章，但是一直一知半解，直到今天看到两篇文章，豁然开朗。没自信写的比这两篇文章更好，只能挑出重点，提出一点自己的看法，当作笔记而已。<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;首先，打开传送门：<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;1. <a><a href="https://www.jianshu.com/p/486b0965c296" target="_blank" rel="noopener">https://www.jianshu.com/p/486b0965c296</a></a><br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2. <a><a href="https://www.jianshu.com/p/dfd940e7fca2" target="_blank" rel="noopener">https://www.jianshu.com/p/dfd940e7fca2</a></a></p><h3 id="linux五种io模型："><a href="#linux五种io模型：" class="headerlink" title="linux五种io模型："></a>linux五种io模型：</h3><ol><li>要点：<ol><li>io等待其实分为两个阶段，一个是等待数据，一个是把数据从内核复制到用户空间。</li><li>select、poll、epoll、recvfrom等都是系统调用，从用户态切换到系统态，有高昂的性能损耗</li><li>进程的阻塞是进程自身的一种主动行为，是为了让出cpu，执行其他任务，不断轮询在系统层面看来本身是一种浪费资源、效率很低的行为，这么做的原因也是因为系统底层没有提供相应的功能，使得上层应用只能采取这种办法。</li><li>这里同步异步是指的这两个阶段有没有联系，如果第一个阶段之后，进程还要等待第二个阶段，那么其实就是同步，异步就是这两个阶段完全没有关系，第一个阶段调用之后，进程就可以不用管了。然后等数据准备好了系统通过回调或信号的方式通知进程，这个过程可以发生在第一阶段之后（信号驱动式IO），也可以发生在第二阶段之后（异步非阻塞 IO）</li></ol></li><li>比较常见的类型：<ol><li>同步阻塞io<ol><li>最常见最简单的类型，linux下默认所有的socket都是阻塞的，io调用的时候两个阶段进程都在等待</li><li>这种模式想要并发就需要多线程，但是并发不可能太大，因为linux默认一个线程是8m的内存空间，所以10000的并发就是80G（在长链接下很可能达到），系统早就爆炸了！被人利用进行ddos，开启一个链接什么都不做都能把服务器撑爆</li></ol></li><li>同步非阻塞<ol><li>socket设置为non-blocking模式，这种情况下，socket调用不会阻塞，会直接返回，然后可以不断调用recvfrom来判断是否有数据来，也可以在轮询之间做点别的事，仔细想想这种代码写出来该有多难看。</li><li>但是，这为同时监控多个socket提供了可能，如果我想提高并发又不想开太多线程可以怎么做呢，可以设置几个队列，read_socket队列，write_socket队列等，每次来请求了，都打开一个socket，然后socket不阻塞，直接返回，放入相应的队列里面，再遍历各个队列，看是否有数据过来，过来就取数据，然后执行相关业务。这种做法有什么缺陷呢：<ol><li>业务代码和底层代码强耦合，重构成本高</li><li>这个轮询的事情不该用户进程来做，每次轮询都会使用recvform系统调用，系统调用的代价很高的，而且还是放在千百次的轮询中，大大的消耗资源</li><li>……</li></ol></li><li>这种模式实现上可以使用两个线程，一个线程监听socket请求，并放入相关队列，一个线程执行轮询，处理业务。</li></ol></li><li>io多路复用<ol><li>上面说了，轮询不该用户进程来做，本身是系统层面没提供相关功能之下的一种妥协，于是就有了select、poll、epoll，这几个与用户进程之间的关系差不多，以select为例，</li><li>这种模式socket也需要设置non-blocking，不然还是会阻塞，有请求过来就把socket扔给select，select调用的时候也会阻塞用户进程，但是多个socket，一旦有到达的，select就会通知用户进程，然后用户进程就可以处理。如果同时来了n个请求，用户进程可以同时把n个请求放进数组让select处理，假设业务处理速度很快，可以瞬间完成，那这n个请求的完成时间则取决于io最慢的那个请求的时间，并发就上来了。</li><li>其实本质上和第二种说的实现差不多，就是要有个角色专门来轮询socket，但是好处是业务代码和底层代码接耦了，然后又不会进行千百次的系统调用，因为遍历在内核中完成了。这种模式的io调用第一步的时间被压缩了，但是第二步，从内核拷贝数据到用户态还是会阻塞</li><li>这种情况，需要进行两部系统调用，一个是select，一个是recvform，所以并发不高的情况，select不一定比同步阻塞加多进程性能好，优势在这种情况下能处理更多的连接</li></ol></li><li>信号驱动式IO<ol><li>这种模式需要socket支持，socket调用的时候安装一个信号处理函数，然后进程就继续运行，数据准备好时，进程会收到一个SIGIO信号，可以在信号处理函数中调用recvform处理数据，这个时候recvform还是阻塞的。因为进程在调用之后不用管后面的数据处理部分，这部分交给信号处理函数来做，所以是异步的。</li></ol></li><li>异步非阻塞 IO<ol><li>进程进行aio_read系统调用之后，会直接返回。然后等数据准备好之后，内核又把数据复制到进场，然后通知进程，在linux中通知的方式是信号。这个时候，io等待的两步都是非阻塞的，目前开源的异步io库有libevent、libev、libuv.  </li></ol></li></ol></li></ol><h3 id="比较select、poll和epoll"><a href="#比较select、poll和epoll" class="headerlink" title="比较select、poll和epoll"></a>比较select、poll和epoll</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;epoll是Linux所特有，在freeBSD中可以使用kqueue。我们说select低效的原因是因为需要遍历fd，在大量连接下比较慢，线性增长，不如epoll的监听回调，此时epoll还是常数级别响应时间（使用哈希表）。但是监听回调本身就有一定的成本，在连接数少并且连接都十分活跃的情况下，select和poll的性能可能比epoll好。</p><ol><li>select<ol><li>select是POSIX所规定，一般操作系统均有实现，</li><li>优点：良好的跨平台性</li><li>缺点：select最大的缺陷就是单个进程所打开的FD是有一定限制的，它由FD_SETSIZE设置，默认值是1024，可以更改，但是会降低效率。同时select是遍历socket。需要维护一个用来存放大量fd的数据结构，这样会使得用户空间和内核空间在传递该结构时复制开销大</li></ol></li><li>poll<ol><li>poll本质上和select没有区别，也是遍历fd。使用“水平触发”，就是如果报告了fd，进程没有处理，下次poll时还会再次报告这个fd</li><li>优点：没有最大连接数的限制，原因是它是基于链表来存储的</li><li>缺点：大量的fd在用户态和内核态传递，</li></ol></li><li>epoll<ol><li>是select和poll的增强版本，epoll使用一个文件描述符管理多个描述符，将用户关系的文件描述符的事件存放到内核的一个事件表中，这样在用户空间和内核空间的copy只需一次，还有一个特点是，epoll使用“事件”的就绪通知方式，通过epoll_ctl注册fd，一旦该fd就绪，内核就会采用类似callback的回调机制来激活该fd，epoll_wait便可以收到通知。</li><li>支持水平触发和边缘触发，水平触发是默认模式。边缘触发就是它只告诉进程哪些fd刚刚变为就绪态，并且只会通知一次。边缘触发是高速工作方式，只支持no-block socket</li><li>优点：<ol><li>没有最大并发连接的限制</li><li>去掉了遍历文件描述符，而是通过监听回调的的机制，所以效率不会随着fd的数目增长而下降，只有活跃的fd才会调用callback</li><li>利用mmap()文件映射内存加速与内核空间的消息传递</li></ol></li></ol></li></ol>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;虽然之前也看过不少关于io多路复用的文章，但是一直一知半解，直到今天看到两篇文章，豁然开朗。没自信写的比这两篇文章更好，只能挑出重点，提出一点自己的看法，当作笔记而已。&lt;br&gt;&amp;nbsp;&amp;nbsp;&amp;
      
    
    </summary>
    
    
      <category term="linux" scheme="https://schecterdamien.github.io/tags/linux/"/>
    
  </entry>
  
  <entry>
    <title>关于语言的杂想</title>
    <link href="https://schecterdamien.github.io/2018/11/19/about-absurd/"/>
    <id>https://schecterdamien.github.io/2018/11/19/about-absurd/</id>
    <published>2018-11-18T16:52:56.000Z</published>
    <updated>2021-03-08T07:43:35.186Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;最近在看西西弗神话，有一些感想。<br>&nbsp;&nbsp;&nbsp;&nbsp;首先，其实没看多少，但是真的是看的很费劲，很头疼。因为，实在是看不明白，这个看不明白我感觉或许不是因为我理解不了，或许是更深层次的无力感。加缪所描述的荒谬感我觉得我应该再能感同身受不过了，我也是通过一些其他途径发现荒谬感这一说法，然后顺着源头找到了加缪。但是结果很沮丧，因为翻译的实在太生硬了，主谓宾都不全，单从词的翻译看来或许还好，但是一旦汇成一个句子，看起来还真的很“荒谬”。或许对于小说来说，翻译错一点不要紧，某种程度上来说小说的“容错率“比较高，因为信息密度不高，但是对一部讲哲学的散文来说，差之毫厘，谬以千里。<br>&nbsp;&nbsp;&nbsp;&nbsp;从另一个角度来看，翻译确实是很难，不同语言之间的表达其实不是一对一的映射。不是说其他一个语言的词映射成中文就是一定是某个词，要是这样，那翻译器的代码我都能写。很多情况是多对多的，比如同样是表示秋天，英文有fall，有autumn，而fall还有”落”的意思，除了映射秋这个季节本身，还额外附带“萧条“、”伤感“、”衰落”之类的主观情感体验，可能上下文语境稍微有点差异，译文的词就有不同的选择，所以翻译更要求，你要想翻译，首先你要完全读懂，明白作者想表达什么，然后翻译。但这是最理想的情况，翻译不可能不带主观感受，你看到的本来就已经是一个二手的理解了，可能区别不大，也可能面目全非，所以有条件有能力的话看书最好还是看原版…<br>&nbsp;&nbsp;&nbsp;&nbsp;上面是从翻译的角度说，从写作的人的角度来说，也不能说没有责任。我发现文学家写的哲学感悟往往和哲学家写的哲学感悟是不一样的。这是感性和理性的区别。感性放佛天生就带有美学的有色眼镜，而作者可能是最为感性的群体之一，而且美学往往一点都不直白，很隐晦，很“曲折”，很多层的转义。所以作家写一个东西的时候，他不会很直白的去写，而是很扭扭捏捏去描写。比如西西弗神话里面有一句很著名的话，“荒诞感，在随便哪条街上，都会直扑随便哪个人的脸上”。初一听，这个表达好牛逼，但是仔细想，应该是想表达荒诞感每个人都会有的对吧？但是我也不敢很肯定是这个意思。于是，无形中增加了我们理解的成本，甚至误解的概率，当然，这种表达是很“美“的，是文学家所追求的。但是哲学家是很“吝啬”的，他们往往单刀直入，直白的让你怀疑这个东西我所理解的真实是他们想表达的吗，所以我感觉哲学家是很理性的，很有逻辑性的。<br>&nbsp;&nbsp;&nbsp;&nbsp;更深层次的 —— 我们可以说都是“巴别塔“惹的祸。圣经有个广为流传的神话，就是古巴比伦人想建造一个能通天的巴别塔，由于大家语言相通，同心协力，高塔直插云端，彷佛没多久真的可以通天了，上帝看到了，就很愤怒，降下灾罚，让人们的语言不再统一，人与人之间难以直接交流，于是最终塔就半途而废了。结合上面说的那些问题，不都是语言的问题吗？语言作为信息的一个载体，两个人要交流，需要一个人写或者说，这个是把自己所想用语言表达出来，这个是表达能力，是编码器，另一个人需要把文字翻译成内心的感受，这个是理解能力，是解码器。编码器和解码器都不是完美的，所以信息就肯定会有失真。所以才会有那么多争吵，很多时候大家表达的东西同一个意思，只不过我编码有问题，或者你解码有问题。同时这也是为什么同一个文化环境下的人之间互相好理解些，因为大家的编码和解码的算法差不多啊，所以误解的概率就小。这还是同一种语言，不同的语言之间还要加了一层“翻译“，那更是痛苦。不得不说，上帝的这个惩罚真是狠，从此没有人能完全理解另一个人，每个人都在信息的孤岛上自说自话…<br>&nbsp;&nbsp;&nbsp;&nbsp;关于语言我隐约还有另一个感觉，现在还不是特别明显，但是已经有了一点迹象。就是我感觉很多时候很多哲学或者终极问题本身，其实就是陷入了语言理解的死循环中。佛法中的一个句式“佛说某某，即非某某，是名某某”，名指的是语言背后的抽象概念，诸子百家就有一个“名家”是专门研究这个的，这个“名”很关键，代表语言所表达的本质。越来越感觉语言是限制我们更深刻理解世界的阻碍。语言是个很高效的理解事物的办法，利于知识的传播，但是表达的准确性是很不够的，以致于我们越来越深陷于语言表面所描绘的描绘的事物。佛学对这一方面的研究很深，对语言很警惕，很多看起来很玄乎的佛学对话其实就是一个意思：我不能说，因为我说的佛法和你理解的佛法肯定不是一个东西，甚至当我说出“佛法”的时候，就错了，佛法本身的定义可能我和你都有差别。“凡所有相,皆是虚妄,若见诸相非相,即见如来”。<br>&nbsp;&nbsp;&nbsp;&nbsp;然后我又想到了禅宗里面讲究顿悟，刚开始我始终理解不了顿悟是什么感觉，有人说，顿悟的感觉就是：有人一直和你说和牛很好吃，特别好吃，你知道了，你也觉得和牛好吃，但是你真的知道吗？直到有一天你真的吃到了和牛，你才感觉到，真他妈好吃！之前我根本就不知道原来是这种味道！大概顿悟就是这种感觉吧。这或许还是语言的锅，好吃有千万种，我无法通过一个“好吃”，准确表达我的感受……这是永远做不到的事。<br>&nbsp;&nbsp;&nbsp;&nbsp;这样越思考就越悲观了，有时候会幻想在圣经的世界里面，上帝发怒之前，人们是怎么一种状态呢，我更愿意相信他们之间连语言都没有…..或者有一种能百分百交流的语言，那真的就是世界大同了，真这样还有人类做不成的事情？难怪上帝会害怕。<br>&nbsp;&nbsp;&nbsp;&nbsp;我相信在很遥远的未来，人类会以另一种形态存在，不在以“语言”这种低效的沟通方式交流，或许那时候，各种“孤独感”、“虚无感”、”荒谬感“就会消失吧。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;最近在看西西弗神话，有一些感想。&lt;br&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;首先，其实没看多少，但是真的是看的很费劲，很头疼。因为，实在是看不明白，这个看不明白我感觉或许不是因为我理解不了，或许是更深层次的无力感。
      
    
    </summary>
    
    
      <category term="杂谈" scheme="https://schecterdamien.github.io/tags/%E6%9D%82%E8%B0%88/"/>
    
  </entry>
  
  <entry>
    <title>判断多个区间是否有重叠</title>
    <link href="https://schecterdamien.github.io/2018/10/10/interval-tree/"/>
    <id>https://schecterdamien.github.io/2018/10/10/interval-tree/</id>
    <published>2018-10-10T10:26:25.000Z</published>
    <updated>2021-03-08T07:43:35.186Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;今天遇到个需求，大致意思是前端给我一堆商品按照阶梯定价的规则，比如订单数为0-200，价格是10块，订单数200-300，价格是8块……简化的数据结构是这样的：  </p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">[</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">"min_orders"</span>: <span class="number">0</span>,</span><br><span class="line">    <span class="attr">"max_orders"</span>: <span class="number">200</span>,</span><br><span class="line">    <span class="attr">"price"</span>: <span class="number">10</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">"min_orders"</span>: <span class="number">200</span>,</span><br><span class="line">    <span class="attr">"max_orders"</span>: <span class="number">300</span>,</span><br><span class="line">    <span class="attr">"price"</span>: <span class="number">8</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">"min_orders"</span>: <span class="number">400</span>,</span><br><span class="line">    <span class="attr">"max_orders"</span>: <span class="number">500</span>,</span><br><span class="line">    <span class="attr">"price"</span>: <span class="number">7</span></span><br><span class="line">  &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;我需要对前端传的数据做校验，其中有一部分就是不同阶的上下界不能有交叉（前端给的阶梯数据不一定有序），比如有0-200的阶梯，如果再有100-300的阶梯那这个数据就有问题，因为没法定价。抽象出来问题其实就是判断多个区间（可能不连续）是否有重叠。<br>&nbsp;&nbsp;&nbsp;&nbsp;初一看，这个问题其实很简单，两个for循环，第一层记住遍历过的区间，第二层把当前区间和之前遍历的区间一个个比较看是否交叉就可以了。这么做当然没问题，但是时间复杂度是O(n^2)，怎么看都觉得应该有更好的解决办法，第一遍的遍历是怎么都省不了的，所以优化到极致也不可能比O(n)低，第二遍其实就是个比较的过程，既然是比较的话，那就有思路了，可以联想到很多类似的问题，本质上和排序问题是差不多的。<br>&nbsp;&nbsp;&nbsp;&nbsp;在stackoverflow看到一个相同的问题：<a href="https://stackoverflow.com/questions/4446112/search-for-interval-overlap-in-list-of-intervals" target="_blank" rel="noopener">search for interval overlap in list of intervals?</a>。里面提到了一个办法：可以先对区间以左端点进行排序，然后再遍历区间，比较当前区间的左端点和上一个区间的右端点，如果当前的左端点比上一个右端点小，那么一定就有重叠的部分。第一遍排序时间复杂度是O(nlogn)，第二遍遍历时间复杂度是O(n)，所以总体的还是O(nlogn)，比如：  </p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[(<span class="number">4</span>,<span class="number">6</span>), (<span class="number">7</span>,<span class="number">9</span>), (<span class="number">1</span>,<span class="number">5</span>), (<span class="number">6</span>,<span class="number">7</span>)]</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;排序完之后是：  </p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[(<span class="number">1</span>,<span class="number">5</span>), (<span class="number">4</span>,<span class="number">6</span>), (<span class="number">6</span>,<span class="number">7</span>), (<span class="number">7</span>,<span class="number">9</span>)]</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;然后进行遍历比较，遍历到第二个区间的时候，左端点是4，比上个区间的右端点5要小，所以肯定有重叠的部分。</p><p>&nbsp;&nbsp;&nbsp;&nbsp;<strong>第二种办法是构建区间树</strong>，上面说的效率最低的那种解决办法，第二层for循环是比较，既然是比较，那么就可以通过树这种结构来提高比较的效率，而不用和前面所有区间进行比较，所以很自然就想到可以构建一个二叉排序树，树的叶节点是每个区间。同时，树的查询效率和树的深度是有关系的，所以构建一个相对平衡的二叉排序树也能进一步提高效率，比如红黑树。算法导论上介绍了这种数据结构，<strong>节点是区间的红黑树——区间树</strong>。<br>&nbsp;&nbsp;&nbsp;&nbsp;红黑树实现起来还是很麻烦的，找了一圈发现github上有现成的轮子——<a href="https://github.com/chaimleib/intervaltree" target="_blank" rel="noopener">intervaltree</a>，使用非常简单：  </p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;</span>&gt; from intervaltree import Interval, IntervalTree</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; tree = IntervalTree()</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; iv = Interval(<span class="number">4</span>, <span class="number">7</span>)</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; tree.add(iv)</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; target_iv = Interval(<span class="number">5</span>, <span class="number">8</span>)</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; tree.overlaps(target_iv)</span><br><span class="line">True</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; target_iv_2 = Interval(<span class="number">7</span>, <span class="number">10</span>)</span><br><span class="line"><span class="meta">&gt;&gt;</span>&gt; tree.overlaps(target_iv_2)</span><br><span class="line">False</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp; 更多用法可以参考<a href="https://pypi.org/project/intervaltree/" target="_blank" rel="noopener">intervaltree · PyPI</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;今天遇到个需求，大致意思是前端给我一堆商品按照阶梯定价的规则，比如订单数为0-200，价格是10块，订单数200-300，价格是8块……简化的数据结构是这样的：  &lt;/p&gt;
&lt;figure class=&quot;highlight j
      
    
    </summary>
    
    
      <category term="algorithm" scheme="https://schecterdamien.github.io/tags/algorithm/"/>
    
  </entry>
  
  <entry>
    <title>使用travis-ci自动部署hexo博客</title>
    <link href="https://schecterdamien.github.io/2018/09/20/ci-test/"/>
    <id>https://schecterdamien.github.io/2018/09/20/ci-test/</id>
    <published>2018-09-20T06:03:58.000Z</published>
    <updated>2021-03-08T07:43:35.184Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;今天又开始折腾了，这次是给博客加上ci功能。ci是Continuous integration的缩写，说人话就是持续集成，具体定义和解释：<a href="https://zh.wikipedia.org/wiki/持續整合" target="_blank" rel="noopener">维基百科</a></p><h3 id="什么是持续集成"><a href="#什么是持续集成" class="headerlink" title="什么是持续集成"></a>什么是持续集成</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;维基上的解释又长又抽象，一句话来说，持续集成是对软件项目进行持续的自动化的编译打包构建测试发布，来检查软件交付质量的一种行为。包含自动构建，自动测试，自动部署等功能，同时还是一种行为规范，就是快速集成代码到主分支，而不是闷头在功能分支上开发，最后合并的时候发现一大堆冲突，也不好测试。一般来说，规模稍大点的公司都会自己搭建一套用于持续集成的服务，比如gitlab提供的ci功能。而对我们大多数托管在github上的项目来说，使用travis-ci就行了。</p><blockquote><p>Travis CI是在软件开发领域中的一个在线的，分布式的持续集成服务，用来构建及测试在GitHub托管的代码。</p></blockquote><h3 id="部署hexo博客"><a href="#部署hexo博客" class="headerlink" title="部署hexo博客"></a>部署hexo博客</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;之前每次部署博客的时候，都需要先<code>hexo g</code>然后再<code>hexo d</code>，貌似也不是很麻烦，但是如果整个构建和部署过程需要很多的命令，一系列的环境变量配置和漫长的等待，那整个过程就会非常痛苦，不要把时间花费在重复无意义的操作上。所以，我想把这两个命令也省略掉，在代码提交的时候，自动触发构建和部署。<br>&nbsp;&nbsp;&nbsp;&nbsp;还有一个问题是，我们使用<code>GitPage</code>的个人站点来搭建博客，博客的静态文件必须在<code>master</code>分支上，那我博客的源码只能放别的分支上，就叫<code>source</code>吧，当然这样别人都能看得到博客源码了，只要源码中不涉及敏感信息，看到就看到吧。<br>&nbsp;&nbsp;&nbsp;&nbsp;首先需要注册travis-ci，直接用github账号授权登录就行，然后选择项目：<br><img src="/2018/09/20/ci-test/1.png"><br>&nbsp;&nbsp;&nbsp;&nbsp;travis-ci构建完后需要把静态文件推到github的master分支，所以需要用户的token，travis-ci才有权限做这些动作。如果之前没有，可以生成一个：<br><img src="/2018/09/20/ci-test/2.png"><br>&nbsp;&nbsp;&nbsp;&nbsp;点击后就会来到下面的界面，先给 Token 起一个名字，然后为它设置一些权限，其中红框内的权限是必须的，其他可以随意添加。<br><img src="/2018/09/20/ci-test/3.png"><br>&nbsp;&nbsp;&nbsp;&nbsp;点击下面的<code>create token</code>按钮，就会生成一个已经赋予好权限的 token 值，然后在环境变量里面设置一个<code>GH_TOKEN</code>的变量，value就是之前的token值，后面会用到这个值<br><img src="/2018/09/20/ci-test/4.jpg"><br>&nbsp;&nbsp;&nbsp;&nbsp;添加<code>.travis.yml</code>配置文件到hexo 源码的根目录下，因为Travis CI 在自动构建时需要获取这些配置信息，以此来完成构建任务；这些配置信息主要包括源码分支，静态文件推送分支，仓库地址等信息。具体配置如下：  </p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">language</span>: node_js</span><br><span class="line"><span class="attribute">node_js</span>: stable</span><br><span class="line"></span><br><span class="line"># S: Build Lifecycle</span><br><span class="line"><span class="attribute">install:</span></span><br><span class="line">  - npm install</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#before_script:</span><br><span class="line"> # - npm install -g gulp</span><br><span class="line"></span><br><span class="line"><span class="attribute">script:</span></span><br><span class="line">  - hexo g</span><br><span class="line"></span><br><span class="line"><span class="attribute">after_script:</span></span><br><span class="line">  - cd ./public</span><br><span class="line">  - git init</span><br><span class="line">  - git config user.name "schecterdamien"</span><br><span class="line">  - git config user.email "schecterfirst@126.com"</span><br><span class="line">  - git add .</span><br><span class="line">  - git commit -m "Update docs"</span><br><span class="line">  - git push --force --quiet "https://$&#123;GH_TOKEN&#125;@$&#123;GH_REF&#125;" master:master</span><br><span class="line"># E: Build LifeCycle</span><br><span class="line"></span><br><span class="line"><span class="attribute">branches:</span></span><br><span class="line">  only:</span><br><span class="line">    - source</span><br><span class="line"><span class="attribute">env:</span></span><br><span class="line"> global:</span><br><span class="line">   - GH_REF: github.com/schecterdamien/schecterdamien.github.io.git</span><br></pre></td></tr></table></figure><p>&nbsp;&nbsp;&nbsp;&nbsp;然后每次在source分支上push代码的时候都会触发自动构建和自动部署：<br><img src="/2018/09/20/ci-test/5.png"> </p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;travis-ci还有很多更多复杂的功能，比如可以设置定时构建部署，比如还可以在配置文件里面设置构建的时候自动跑测试，对于小小的博客来说，这些必要性不大。还有就是一些敏感的信息，比如很多第三方服务的secret、token之类环境变量的千万不能直接写在配置文件里面，因为代码已经上传到github上了，所以别人都能看到到这些信息，这些可以在travis-ci项目设置中的<code>Environment Variables</code> 设置，如上面的<code>GH_REF</code>设置。</p><h4 id="本文参考链接："><a href="#本文参考链接：" class="headerlink" title="本文参考链接："></a>本文参考链接：</h4><ul><li><a href="https://www.cnblogs.com/dmego/p/7664877.html" target="_blank" rel="noopener">https://www.cnblogs.com/dmego/p/7664877.html</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;今天又开始折腾了，这次是给博客加上ci功能。ci是Continuous integration的缩写，说人话就是持续集成，具体定义和解释：&lt;a href=&quot;https://zh.wikipedia.org/wiki/持續整合&quot;
      
    
    </summary>
    
    
      <category term="devops" scheme="https://schecterdamien.github.io/tags/devops/"/>
    
  </entry>
  
  <entry>
    <title>我的第一篇博客</title>
    <link href="https://schecterdamien.github.io/2018/09/19/first-post/"/>
    <id>https://schecterdamien.github.io/2018/09/19/first-post/</id>
    <published>2018-09-19T09:25:19.000Z</published>
    <updated>2021-03-08T07:43:35.184Z</updated>
    
    <content type="html"><![CDATA[<h3 id="折腾博客时的一些感想"><a href="#折腾博客时的一些感想" class="headerlink" title="折腾博客时的一些感想"></a>折腾博客时的一些感想</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;折腾了好久，博客终于搭出个大概，有个能看的样子，本来用github+hexo就可以很简单搞定的问题，为什么会浪费这么多时间呢，因为一刚开始想的是自己独立建站，完全自己维护。<br>&nbsp;&nbsp;&nbsp;&nbsp;首先，得有个服务器吧，还要有个域名吧。要存文章，tag，评论什么的肯定还要数据库吧，postgresql这么先进，就他吧。后端就用熟悉的flask吧，貌似最近还出了个flask-rest，可以自动生成restful风格的接口，嗯，拿来用用学习下，源码好像不多，要不趁机都看看。部署需要的工具链gunicorn、supervisor、nginx这几个肯定是要吧。对了，还有前端，正好学学前端比较火的框架，就vue吧，主要是比react简单一点，现在不都流行前后端完全分离吗，再小的项目也要有个好的架构啊，不然以后拓展就麻烦了！那就再加个node做中间层转发。好像差不多，对了还要做好安全方面的措施，还有容灾备份啊，而且博客最重要就是页面的设计，这个得好好参考下，是不是要买本设计的书看看…………<br>&nbsp;&nbsp;&nbsp;&nbsp;结果，最后就是拖了几个月，就买了服务器、域名、设计了表，其他的一直无限期拖延。仔细想想，貌似这是我一直以来的毛病，分析这个过程，又引出了更多的感想。</p><h3 id="为什么要写博客"><a href="#为什么要写博客" class="headerlink" title="为什么要写博客"></a>为什么要写博客</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;为什么要写博客，因为很多知识需要沉淀下来，需要“持久化”，人的大脑是快，就像内存，但是容量有限，很多知识不及时记下来的话，就会丢失，那其实学了和没学一样，对自身技术的进步不利。另一方面，想法也是一样，想法不沉淀下来，一闪而过，其实也相当于没出现，写下来也有助于思考，写出来的东西永远都是思考过一遍的，自我加工的。更重要的是，如果不经常写，那这种写的能力就会退化，到了最后内心澎湃万千，下笔或出口都平平淡淡，甚至会错位。那，其实对我来说，重要是要写东西，而不是使劲折腾怎么搭建博客，当然博客漂亮对我来说也重要，别人看着舒服，我自己写着也开心，但是现在在这上面浪费了太多的时间，以至最终什么都没写，实在是本末倒置。</p><h3 id="方法论"><a href="#方法论" class="headerlink" title="方法论"></a>方法论</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;做事情，方法论很重要，我需要的是尽快搭建起一个博客可以写东西，但是上面的方案里面，vue以及页面设计我完全不熟，在这上面卡的时间是最多的。仔细想，我其实是在搭博客的过程中掺杂了很多其他的期望，比如能借此学习到很多新的技术，但是没有仔细思考可能付出的成本，在构想方案的时候我并没有意识到这点，于是无形中提高了门槛，最终又没有一个得到适当的正反馈，导致一再拖延（缺少正反馈是所有拖延的主要原因），所以这个做事情的方法论是有问题的，我应该是先用自己熟悉的技术，自己掌握的住的来快速实现一版，迅速看到效果，得到一个正反馈，然后再反复迭代优化。而且搭建博客和学习新技术其实是两件事，但是我却当成了一件事来做。当然，如果能hold住，那两者可以兼得，这也源于自己没有正确的评估成本。  </p><h3 id="坚持"><a href="#坚持" class="headerlink" title="坚持"></a>坚持</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;一直觉得时间的力量是很强大的，能让渺小变得伟大。一件很小的事情能够坚持做很久（比如几十年如一日的每天坚持跑步）那比在短时间内完成让人惊讶的事情（比如骑行西藏、突击熬夜复习得满分）难度更大，更显的伟大。其实这不是我第一个博客，之前还有个博客，最后死于没有坚持。我知道这很难，所以我不打算定个计划说每周一定要更新几篇，也许我会几周不写，也可能感觉来了一天写几篇，我并没有赋予博客其他的意义和目的性，也不想达成一个每天都坚持写博客的伟大成就，不过放在更广的时间维度上来看，这事我一直在坚持做，写的也是我想写的，最后也没法放弃，或许会让我觉得自己很厉害吧，哈哈。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h3 id=&quot;折腾博客时的一些感想&quot;&gt;&lt;a href=&quot;#折腾博客时的一些感想&quot; class=&quot;headerlink&quot; title=&quot;折腾博客时的一些感想&quot;&gt;&lt;/a&gt;折腾博客时的一些感想&lt;/h3&gt;&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;折腾了好久，博客终于搭出个大
      
    
    </summary>
    
    
      <category term="杂谈" scheme="https://schecterdamien.github.io/tags/%E6%9D%82%E8%B0%88/"/>
    
  </entry>
  
</feed>
